# v0.4.0 WPF MVVM 重构 - 详细设计和开发计划

> 文档版本：1.0.0  
> 创建日期：2026-05-08  
> 预计工期：3 周（15个工作日）  
> 状态：规划中

---

## 1. 概述

### 1.1 目标

将 ReportDataMaker 重构为标准 MVVM 架构，引入 HandyControl 美化 UI，实现 TabControl 驱动适配器页签管理，可折叠左面板。

### 1.2 核心价值

- **标准化架构**：完整的 MVVM 模式，View 和 ViewModel 解耦
- **现代化 UI**：HandyControl 组件库，开箱即用的主题和动画
- **可扩展性**：TabControl 驱动，每个适配器独立 Tab
- **可测试性**：依赖注入，服务可替换

### 1.3 前置依赖

- v0.3.0 架构统一（Contracts 项目）✅

---

## 2. 技术选型

| 组件 | 选择 | 版本 | 说明 |
|------|------|------|------|
| UI 框架 | HandyControl | 3.5.1 | 现代化 WPF 控件库 |
| DI 容器 | Microsoft.Extensions.DependencyInjection | 8.0.1 | .NET 标准 |
| 主题 | HandyControl SkinDefault | - | 默认主题 |

---

## 3. 项目结构

### 3.1 目标目录结构

```
ReportDataMaker/
├── App.xaml                              # 启动 + DI + HandyControl 主题
├── App.xaml.cs
│
├── Infrastructure/                        # MVVM 基础设施
│   ├── ViewModelBase.cs
│   ├── RelayCommand.cs
│   ├── AsyncRelayCommand.cs
│   ├── IDialogService.cs
│   └── ServiceLocator.cs
│
├── Models/                               # 数据模型（引用 Contracts）
│
├── Services/                             # 业务服务
│   ├── ITemplateLoaderService.cs
│   ├── TemplateLoaderService.cs
│   ├── ITemplatePreviewService.cs
│   ├── TemplatePreviewService.cs
│   ├── IDataBindingService.cs
│   ├── DataBindingService.cs
│   └── AdapterConfigStore.cs
│
├── ViewModels/
│   ├── MainViewModel.cs                  # Shell 编排器
│   ├── TemplateLoadViewModel.cs          # 模板加载对话框
│   ├── SidePanelViewModel.cs             # 左侧面板状态
│   │
│   ├── Tabs/                            # Tab 页 ViewModel
│   │   ├── TabViewModelBase.cs          # Tab 基类
│   │   ├── DataEntryTabViewModel.cs     # 数据录入 Tab
│   │   └── PreviewTabViewModel.cs       # 预览 Tab
│   │
│   └── Dialogs/                         # 对话框 ViewModel
│       └── AddAdapterDialogViewModel.cs # 添加适配器对话框
│
├── Views/
│   ├── MainWindow.xaml                  # Shell 布局
│   ├── MainWindow.xaml.cs
│   │
│   ├── Dialogs/                         # 对话框视图
│   │   ├── TemplateLoadDialog.xaml
│   │   ├── TemplateLoadDialog.xaml.cs
│   │   ├── AddAdapterDialog.xaml
│   │   └── AddAdapterDialog.xaml.cs
│   │
│   └── Tabs/                            # Tab 页视图
│       ├── DataEntryTab.xaml
│       ├── DataEntryTab.xaml.cs
│       ├── PreviewTab.xaml
│       └── PreviewTab.xaml.cs
│
├── Converters/                          # 值转换器
│   └── BoolToVisibilityConverter.cs
│
└── Resources/
    └── Styles.xaml                       # 自定义样式
```

---

## 4. 详细实施步骤

### 阶段一：基础设施（Day 1-2）

#### Step 1.1：添加 NuGet 依赖

修改 `ReportDataMaker.csproj`：

```xml
<ItemGroup>
    <PackageReference Include="HandyControl" Version="3.5.1" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.0.1" />
</ItemGroup>
```

#### Step 1.2：创建 MVVM 基础设施

**新建文件**：`Infrastructure/ViewModelBase.cs`

```csharp
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace ReportDataMaker.Infrastructure;

public abstract class ViewModelBase : INotifyPropertyChanged
{
    public event PropertyChangedEventHandler? PropertyChanged;

    protected virtual void OnPropertyChanged([CallerMemberName] string? propertyName = null)
    {
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
    }

    protected bool SetProperty<T>(ref T field, T value, [CallerMemberName] string? propertyName = null)
    {
        if (EqualityComparer<T>.Default.Equals(field, value))
            return false;

        field = value;
        OnPropertyChanged(propertyName);
        return true;
    }
}
```

**新建文件**：`Infrastructure/RelayCommand.cs`

```csharp
using System.Windows.Input;

namespace ReportDataMaker.Infrastructure;

public class RelayCommand : ICommand
{
    private readonly Action<object?> _execute;
    private readonly Func<object?, bool>? _canExecute;

    public RelayCommand(Action<object?> execute, Func<object?, bool>? canExecute = null)
    {
        _execute = execute ?? throw new ArgumentNullException(nameof(execute));
        _canExecute = canExecute;
    }

    public event EventHandler? CanExecuteChanged
    {
        add => CommandManager.RequerySuggested += value;
        remove => CommandManager.RequerySuggested -= value;
    }

    public bool CanExecute(object? parameter) => _canExecute?.Invoke(parameter) ?? true;

    public void Execute(object? parameter) => _execute(parameter);

    public void RaiseCanExecuteChanged() => CommandManager.InvalidateRequerySuggested();
}
```

**新建文件**：`Infrastructure/AsyncRelayCommand.cs`

```csharp
using System.Windows.Input;

namespace ReportDataMaker.Infrastructure;

public class AsyncRelayCommand : ICommand
{
    private readonly Func<object?, Task> _execute;
    private readonly Func<object?, bool>? _canExecute;
    private bool _isExecuting;

    public AsyncRelayCommand(Func<object?, Task> execute, Func<object?, bool>? canExecute = null)
    {
        _execute = execute ?? throw new ArgumentNullException(nameof(execute));
        _canExecute = canExecute;
    }

    public AsyncRelayCommand(Action<object?> execute, Func<object?, bool>? canExecute = null)
        : this(p => { execute(p); return Task.CompletedTask; }, canExecute)
    {
    }

    public event EventHandler? CanExecuteChanged
    {
        add => CommandManager.RequerySuggested += value;
        remove => CommandManager.RequerySuggested -= value;
    }

    public bool IsExecuting
    {
        get => _isExecuting;
        private set
        {
            _isExecuting = value;
            CanExecuteChanged?.Invoke(this, EventArgs.Empty);
        }
    }

    public bool CanExecute(object? parameter) => !IsExecuting && (_canExecute?.Invoke(parameter) ?? true);

    public async void Execute(object? parameter)
    {
        if (IsExecuting) return;
        IsExecuting = true;
        try
        {
            await _execute(parameter);
        }
        finally
        {
            IsExecuting = false;
        }
    }

    public void RaiseCanExecuteChanged() => CommandManager.InvalidateRequerySuggested();
}
```

**新建文件**：`Infrastructure/IDialogService.cs`

```csharp
namespace ReportDataMaker.Infrastructure;

public interface IDialogService
{
    string? OpenFile(string filter, string title);
    string? SaveFile(string filter, string title, string? defaultName = null);
    bool Confirm(string message, string title);
    void ShowInfo(string message, string title);
    void ShowError(string message, string title);
    void ShowSuccess(string message, string title = "成功");
}
```

**新建文件**：`Infrastructure/DialogService.cs`

```csharp
using System.Windows;
using Microsoft.Win32;
using HandyControl.Controls;

namespace ReportDataMaker.Infrastructure;

public class DialogService : IDialogService
{
    public string? OpenFile(string filter, string title)
    {
        var dialog = new OpenFileDialog
        {
            Filter = filter,
            Title = title
        };
        return dialog.ShowDialog() == true ? dialog.FileName : null;
    }

    public string? SaveFile(string filter, string title, string? defaultName = null)
    {
        var dialog = new SaveFileDialog
        {
            Filter = filter,
            Title = title,
            FileName = defaultName ?? string.Empty
        };
        return dialog.ShowDialog() == true ? dialog.FileName : null;
    }

    public bool Confirm(string message, string title)
    {
        return HandyControl.Controls.MessageBox.Ask(message, title) == MessageBoxResult.OK;
    }

    public void ShowInfo(string message, string title)
    {
        HandyControl.Controls.MessageBox.Info(message, title);
    }

    public void ShowError(string message, string title)
    {
        HandyControl.Controls.MessageBox.Error(message, title);
    }

    public void ShowSuccess(string message, string title = "成功")
    {
        Growl.Success(message, title);
    }
}
```

#### Step 1.3：创建服务定位器

**新建文件**：`Infrastructure/ServiceLocator.cs`

```csharp
using Microsoft.Extensions.DependencyInjection;

namespace ReportDataMaker.Infrastructure;

public static class ServiceLocator
{
    private static IServiceProvider? _serviceProvider;

    public static IServiceProvider Services => _serviceProvider 
        ?? throw new InvalidOperationException("ServiceLocator not initialized");

    public static void Initialize(IServiceCollection services)
    {
        _serviceProvider = services.BuildServiceProvider();
    }

    public static T GetService<T>() where T : class
    {
        return Services.GetRequiredService<T>();
    }

    public static T? GetServiceOrDefault<T>() where T : class
    {
        return Services.GetService<T>();
    }
}
```

#### Step 1.4：配置 App.xaml

**修改文件**：`App.xaml`

```xml
<Application x:Class="ReportDataMaker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
```

**修改文件**：`App.xaml.cs`

```csharp
using System.Windows;
using Microsoft.Extensions.DependencyInjection;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Services;
using ReportDataMaker.ViewModels;
using ReportDataMaker.Views;

namespace ReportDataMaker;

public partial class App : Application
{
    protected override void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);

        var services = new ServiceCollection();

        // 基础设施
        services.AddSingleton<IDialogService, DialogService>();

        // 业务服务
        services.AddSingleton<ITemplateLoaderService, TemplateLoaderService>();
        services.AddSingleton<ITemplatePreviewService, TemplatePreviewService>();
        services.AddSingleton<IDataBindingService, DataBindingService>();
        services.AddSingleton<AdapterConfigStore>();

        // ViewModels
        services.AddTransient<MainViewModel>();
        services.AddTransient<TemplateLoadViewModel>();
        services.AddTransient<SidePanelViewModel>();
        services.AddTransient<DataEntryTabViewModel>();
        services.AddTransient<PreviewTabViewModel>();
        services.AddTransient<AddAdapterDialogViewModel>();

        ServiceLocator.Initialize(services);

        var mainWindow = new MainWindow
        {
            DataContext = ServiceLocator.GetService<MainViewModel>()
        };
        
        mainWindow.Show();
    }
}
```

### 阶段二：主界面布局（Day 3-5）

#### Step 2.1：创建 MainWindow.xaml

**新建文件**：`Views/MainWindow.xaml`

```xml
<hc:Window x:Class="ReportDataMaker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ReportDataMaker.Views"
        xmlns:tabs="clr-namespace:ReportDataMaker.Views.Tabs"
        xmlns:converters="clr-namespace:ReportDataMaker.Converters"
        Title="杏林报告平台 - 数据录入工具"
        Height="720" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <converters:BoolToWidthConverter x:Key="BoolToWidthConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        
        <!-- 隐式 DataTemplate -->
        <DataTemplate DataType="{x:Type vm:DataEntryTabViewModel}">
            <tabs:DataEntryTab/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PreviewTabViewModel}">
            <tabs:PreviewTab/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 菜单栏 -->
        <Menu Grid.Row="0" Background="White">
            <MenuItem Header="文件">
                <MenuItem Header="加载模板" Command="{Binding LoadTemplateCommand}"/>
                <Separator/>
                <MenuItem Header="保存" Command="{Binding SaveCommand}"/>
                <MenuItem Header="另存为..." Command="{Binding SaveAsCommand}"/>
                <Separator/>
                <MenuItem Header="退出" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="适配器">
                <MenuItem Header="添加适配器" Command="{Binding AddAdapterCommand}"/>
                <Separator/>
                <MenuItem Header="从文件导入配置..."/>
                <MenuItem Header="导出配置到文件..."/>
            </MenuItem>
            <MenuItem Header="视图">
                <MenuItem Header="切换侧边栏" Command="{Binding ToggleSidePanelCommand}"/>
            </MenuItem>
            <MenuItem Header="帮助">
                <MenuItem Header="关于"/>
            </MenuItem>
        </Menu>

        <!-- 主工作区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧面板 -->
            <Border Grid.Column="0" 
                    Background="#F5F5F5" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="0,0,1,0">
                <Grid Width="{Binding IsSidePanelExpanded, 
                             Converter={StaticResource BoolToWidthConverter},
                             ConverterParameter='240,48'}">
                    
                    <!-- 折叠按钮列 -->
                    <StackPanel Visibility="{Binding IsSidePanelExpanded, 
                                           Converter={StaticResource InverseBoolToVisibility}}"
                                VerticalAlignment="Top" Margin="0,8,0,0">
                        <Button Command="{Binding ToggleSidePanelCommand}"
                                Style="{StaticResource ButtonIcon}"
                                hc:IconElement.Kind="ChevronRight"
                                ToolTip="展开侧边栏"/>
                    </StackPanel>

                    <!-- 完整面板 -->
                    <DockPanel Visibility="{Binding IsSidePanelExpanded, 
                                          Converter={StaticResource BoolToVisibility}}">
                        
                        <!-- 折叠按钮 -->
                        <Button DockPanel.Dock="Top"
                                Command="{Binding ToggleSidePanelCommand}"
                                Style="{StaticResource ButtonIcon}"
                                HorizontalAlignment="Right"
                                Margin="0,4,4,0"
                                hc:IconElement.Kind="ChevronLeft"
                                ToolTip="折叠侧边栏"/>

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="8">
                                
                                <!-- 模板信息 -->
                                <hc:Expander Header="模板信息" IsExpanded="True" Margin="0,0,0,8">
                                    <StackPanel Margin="8">
                                        <TextBlock Text="{Binding TemplateName, FallbackValue='未加载模板'}"
                                                   FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding TemplateVersion, StringFormat='版本: {0}'}"
                                                   Foreground="Gray" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding FieldSummary, StringFormat='字段: {0}'}"
                                                   Foreground="Gray" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </hc:Expander>

                                <!-- 适配器列表 -->
                                <hc:Expander Header="已配置适配器" IsExpanded="True" Margin="0,0,0,8">
                                    <StackPanel Margin="8">
                                        <ItemsControl ItemsSource="{Binding Adapters}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                            Command="{Binding DataContext.SwitchToAdapterTabCommand, 
                                                                     RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="0,2"
                                                            HorizontalContentAlignment="Left"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <Button Content="+ 添加适配器" 
                                                Command="{Binding AddAdapterCommand}"
                                                Style="{StaticResource ButtonDashed}"
                                                Margin="0,4,0,0"/>
                                    </StackPanel>
                                </hc:Expander>

                                <!-- 快速操作 -->
                                <hc:Expander Header="操作" IsExpanded="True">
                                    <StackPanel Margin="8">
                                        <Button Content="保存配置" 
                                                Command="{Binding SaveCommand}"
                                                Margin="0,2"/>
                                        <Button Content="导出报告" 
                                                Command="{Binding ExportCommand}"
                                                Margin="0,2"/>
                                        <Button Content="批量生成" 
                                                Command="{Binding BatchExportCommand}"
                                                Margin="0,2"/>
                                    </StackPanel>
                                </hc:Expander>

                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Grid>
            </Border>

            <!-- 中央 Tab 区域 -->
            <hc:TabControl Grid.Column="1"
                          Style="{StaticResource TabControlCapsule}"
                          ItemsSource="{Binding Tabs}"
                          SelectedItem="{Binding ActiveTab}"
                          Margin="8">
                <hc:TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                            <Button Command="{Binding CloseCommand}"
                                    Visibility="{Binding IsClosable, 
                                                 Converter={StaticResource BoolToVisibilityConverter}}"
                                    Style="{StaticResource ButtonIcon}"
                                    Margin="8,0,0,0"
                                    Padding="2"
                                    hc:IconElement.Kind="Close"
                                    Width="16" Height="16"/>
                        </StackPanel>
                    </DataTemplate>
                </hc:TabControl.ItemTemplate>
                <hc:TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" Margin="0,8,0,0"/>
                    </DataTemplate>
                </hc:TabControl.ContentTemplate>
            </hc:TabControl>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding StatusText}"/>
                </StackPanel>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusInfo}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</hc:Window>
```

**新建文件**：`Views/MainWindow.xaml.cs`

```csharp
using System.Windows;
using HandyControl.Controls;

namespace ReportDataMaker.Views;

public partial class MainWindow : HandyControl.Controls.Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
```

### 阶段三：ViewModel 实现（Day 6-8）

#### Step 3.1：创建 Tab 基类

**新建文件**：`ViewModels/Tabs/TabViewModelBase.cs`

```csharp
using ReportDataMaker.Infrastructure;

namespace ReportDataMaker.ViewModels.Tabs;

public abstract class TabViewModelBase : ViewModelBase
{
    private string _title = string.Empty;
    public string Title
    {
        get => _title;
        set => SetProperty(ref _title, value);
    }

    private bool _isClosable = true;
    public bool IsClosable
    {
        get => _isClosable;
        set => SetProperty(ref _isClosable, value);
    }

    private bool _isSelected;
    public bool IsSelected
    {
        get => _isSelected;
        set => SetProperty(ref _isSelected, value);
    }

    public RelayCommand? CloseCommand { get; protected set; }
    public event Action<TabViewModelBase>? CloseRequested;
    
    protected void RequestClose()
    {
        CloseRequested?.Invoke(this);
    }
}
```

#### Step 3.2：创建 DataEntryTabViewModel

**新建文件**：`ViewModels/Tabs/DataEntryTabViewModel.cs`

```csharp
using System.Collections.ObjectModel;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Models;
using ReportDataMaker.Services;

namespace ReportDataMaker.ViewModels.Tabs;

public class DataEntryTabViewModel : TabViewModelBase
{
    private readonly IDataBindingService _dataBindingService;
    
    public ExternalTemplateDefinition? Template { get; }
    
    public ObservableCollection<FieldViewModel> Fields { get; } = new();

    public DataEntryTabViewModel(ExternalTemplateDefinition template, IDataBindingService dataBindingService)
    {
        _dataBindingService = dataBindingService;
        Template = template;
        Title = "数据录入";
        IsClosable = false;
        
        LoadFields();
    }

    private void LoadFields()
    {
        if (Template?.Elements == null) return;
        
        Fields.Clear();
        foreach (var element in Template.Elements)
        {
            if (!string.IsNullOrEmpty(element.DataPath))
            {
                Fields.Add(new FieldViewModel
                {
                    ElementId = element.Id,
                    Label = element.Label,
                    DataPath = element.DataPath,
                    Value = element.DefaultValue
                });
            }
        }
    }

    public void ApplyDataFromAdapter(string dataPath, object value)
    {
        var field = Fields.FirstOrDefault(f => f.DataPath == dataPath);
        if (field != null)
        {
            field.Value = value?.ToString() ?? string.Empty;
        }
    }
}

public class FieldViewModel : ViewModelBase
{
    public string ElementId { get; set; } = string.Empty;
    public string Label { get; set; } = string.Empty;
    public string DataPath { get; set; } = string.Empty;

    private string _value = string.Empty;
    public string Value
    {
        get => _value;
        set => SetProperty(ref _value, value);
    }
}
```

#### Step 3.3：创建 PreviewTabViewModel

**新建文件**：`ViewModels/Tabs/PreviewTabViewModel.cs`

```csharp
using System.Windows.Media;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Models;
using ReportDataMaker.Services;

namespace ReportDataMaker.ViewModels.Tabs;

public class PreviewTabViewModel : TabViewModelBase
{
    private readonly ITemplatePreviewService _previewService;
    
    public ExternalTemplateDefinition? Template { get; }
    
    private Visual? _previewVisual;
    public Visual? PreviewVisual
    {
        get => _previewVisual;
        private set => SetProperty(ref _previewVisual, value);
    }

    public PreviewTabViewModel(ExternalTemplateDefinition template, ITemplatePreviewService previewService)
    {
        _previewService = previewService;
        Template = template;
        Title = "预览";
        IsClosable = false;
    }

    public void RefreshPreview()
    {
        if (Template == null) return;
        PreviewVisual = _previewService.GeneratePreview(Template);
    }
}
```

#### Step 3.4：创建 MainViewModel

**新建文件**：`ViewModels/MainViewModel.cs`

```csharp
using System.Collections.ObjectModel;
using System.Windows.Input;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Models;
using ReportDataMaker.Services;
using ReportDataMaker.ViewModels.Tabs;
using Xinglin.ReportEditor.Contracts.Enums;

namespace ReportDataMaker.ViewModels;

public class MainViewModel : ViewModelBase
{
    private readonly ITemplateLoaderService _templateLoader;
    private readonly IDataBindingService _dataBindingService;
    private readonly ITemplatePreviewService _previewService;
    private readonly IDialogService _dialogService;
    private readonly AdapterConfigStore _configStore;

    public MainViewModel(
        ITemplateLoaderService templateLoader,
        IDataBindingService dataBindingService,
        ITemplatePreviewService previewService,
        IDialogService dialogService,
        AdapterConfigStore configStore)
    {
        _templateLoader = templateLoader;
        _dataBindingService = dataBindingService;
        _previewService = previewService;
        _dialogService = dialogService;
        _configStore = configStore;

        Tabs = new ObservableCollection<TabViewModelBase>();
        Adapters = new ObservableCollection<AdapterItemViewModel>();

        LoadTemplateCommand = new RelayCommand(ExecuteLoadTemplate);
        ToggleSidePanelCommand = new RelayCommand(_ => IsSidePanelExpanded = !IsSidePanelExpanded);
        AddAdapterCommand = new RelayCommand(ExecuteAddAdapter);
        SwitchToAdapterTabCommand = new RelayCommand(ExecuteSwitchToAdapter);
        SaveCommand = new AsyncRelayCommand(ExecuteSaveAsync, _ => IsTemplateLoaded);
        ExitCommand = new RelayCommand(_ => System.Windows.Application.Current.Shutdown());

        // 加载最近使用的模板或显示加载对话框
        ShowTemplateLoadDialog();
    }

    // 模板状态
    private ExternalTemplateDefinition? _currentTemplate;
    public ExternalTemplateDefinition? CurrentTemplate
    {
        get => _currentTemplate;
        private set
        {
            SetProperty(ref _currentTemplate, value);
            IsTemplateLoaded = value != null;
        }
    }

    private bool _isTemplateLoaded;
    public bool IsTemplateLoaded
    {
        get => _isTemplateLoaded;
        private set => SetProperty(ref _isTemplateLoaded, value);
    }

    public string TemplateName => CurrentTemplate?.Name ?? "未加载模板";
    public string TemplateVersion => CurrentTemplate != null ? $"v{CurrentTemplate.Version}" : "";

    public string FieldSummary
    {
        get
        {
            if (CurrentTemplate?.Elements == null) return "";
            var groups = CurrentTemplate.Elements
                .GroupBy(e => e.Group)
                .ToDictionary(g => g.Key, g => g.Count());
            return string.Join(" | ", groups.Select(kv => 
                $"{kv.Key}:{kv.Value}"));
        }
    }

    // 侧边栏状态
    private bool _isSidePanelExpanded = true;
    public bool IsSidePanelExpanded
    {
        get => _isSidePanelExpanded;
        set => SetProperty(ref _isSidePanelExpanded, value);
    }

    // 适配器列表
    public ObservableCollection<AdapterItemViewModel> Adapters { get; }

    // Tab 管理
    public ObservableCollection<TabViewModelBase> Tabs { get; }

    private TabViewModelBase? _activeTab;
    public TabViewModelBase? ActiveTab
    {
        get => _activeTab;
        set => SetProperty(ref _activeTab, value);
    }

    // 状态
    private string _statusText = "就绪";
    public string StatusText
    {
        get => _statusText;
        set => SetProperty(ref _statusText, value);
    }

    private string _statusInfo = "";
    public string StatusInfo
    {
        get => _statusInfo;
        set => SetProperty(ref _statusInfo, value);
    }

    public Brush StatusColor => IsTemplateLoaded ? Brushes.Green : Brushes.Gray;

    // 命令
    public ICommand LoadTemplateCommand { get; }
    public ICommand ToggleSidePanelCommand { get; }
    public ICommand AddAdapterCommand { get; }
    public ICommand SwitchToAdapterTabCommand { get; }
    public ICommand SaveCommand { get; }
    public ICommand ExitCommand { get; }

    private void ShowTemplateLoadDialog()
    {
        var dialogVm = ServiceLocator.GetService<TemplateLoadViewModel>();
        dialogVm.TemplateSelected += OnTemplateSelected;
        
        var dialog = new Views.Dialogs.TemplateLoadDialog
        {
            DataContext = dialogVm,
            Owner = System.Windows.Application.Current.MainWindow
        };
        dialog.ShowDialog();
    }

    private void OnTemplateSelected(ExternalTemplateDefinition template)
    {
        LoadTemplate(template);
    }

    private void ExecuteLoadTemplate(object? parameter)
    {
        ShowTemplateLoadDialog();
    }

    private void LoadTemplate(ExternalTemplateDefinition template)
    {
        CurrentTemplate = template;
        StatusInfo = $"模板: {template.Name}";

        // 初始化固定 Tab
        Tabs.Clear();
        
        var dataEntryTab = new DataEntryTabViewModel(template, _dataBindingService);
        dataEntryTab.CloseRequested += OnTabCloseRequested;
        Tabs.Add(dataEntryTab);

        var previewTab = new PreviewTabViewModel(template, _previewService);
        previewTab.CloseRequested += OnTabCloseRequested;
        Tabs.Add(previewTab);

        // 加载已保存的适配器配置
        var savedConfigs = _configStore.Load(template.Name);
        foreach (var config in savedConfigs)
        {
            AddAdapterTab(config);
        }

        ActiveTab = Tabs[0];

        OnPropertyChanged(nameof(TemplateName));
        OnPropertyChanged(nameof(TemplateVersion));
        OnPropertyChanged(nameof(FieldSummary));
    }

    private void ExecuteAddAdapter(object? parameter)
    {
        var dialogVm = ServiceLocator.GetService<AddAdapterDialogViewModel>();
        
        var dialog = new Views.Dialogs.AddAdapterDialog
        {
            DataContext = dialogVm,
            Owner = System.Windows.Application.Current.MainWindow
        };
        
        if (dialog.ShowDialog() == true && dialogVm.SelectedAdapterType != null)
        {
            var config = new AdapterConfigBase
            {
                AdapterId = Guid.NewGuid().ToString("N"),
                Type = dialogVm.SelectedAdapterType.Value,
                DisplayName = dialogVm.AdapterName,
                IsEnabled = true
            };
            AddAdapterTab(config);
        }
    }

    private void AddAdapterTab(AdapterConfigBase config)
    {
        // 临时实现，后续扩展
        var tab = new DataEntryTabViewModel(CurrentTemplate!, _dataBindingService)
        {
            Title = $"适配器: {config.DisplayName}",
            IsClosable = true
        };
        tab.CloseRequested += OnTabCloseRequested;
        
        Tabs.Insert(Tabs.Count - 1, tab);
        Adapters.Add(new AdapterItemViewModel(config));
        ActiveTab = tab;
    }

    private void ExecuteSwitchToAdapter(object? parameter)
    {
        if (parameter is AdapterItemViewModel adapter)
        {
            var tab = Tabs.FirstOrDefault(t => t.Title.Contains(adapter.DisplayName));
            if (tab != null)
            {
                ActiveTab = tab;
            }
        }
    }

    private void OnTabCloseRequested(TabViewModelBase tab)
    {
        if (tab.IsClosable)
        {
            Tabs.Remove(tab);
            Adapters.Remove(Adapters.FirstOrDefault(a => tab.Title.Contains(a.DisplayName)));
        }
    }

    private async Task ExecuteSaveAsync(object? parameter)
    {
        try
        {
            StatusText = "保存中...";
            await Task.Delay(100); // 模拟保存
            _dialogService.ShowSuccess("配置已保存");
            StatusText = "已保存";
        }
        catch (Exception ex)
        {
            _dialogService.ShowError($"保存失败: {ex.Message}");
            StatusText = "保存失败";
        }
    }
}

public class AdapterItemViewModel : ViewModelBase
{
    public string AdapterId { get; }
    public string DisplayName { get; }
    public AdapterType Type { get; }

    public AdapterItemViewModel(AdapterConfigBase config)
    {
        AdapterId = config.AdapterId;
        DisplayName = config.DisplayName;
        Type = config.Type;
    }
}
```

#### Step 3.5：创建 TemplateLoadViewModel

**新建文件**：`ViewModels/TemplateLoadViewModel.cs`

```csharp
using System.Collections.ObjectModel;
using System.Windows.Input;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Models;
using ReportDataMaker.Services;

namespace ReportDataMaker.ViewModels;

public class TemplateLoadViewModel : ViewModelBase
{
    private readonly ITemplateLoaderService _templateLoader;
    private readonly IDialogService _dialogService;

    public TemplateLoadViewModel(ITemplateLoaderService templateLoader, IDialogService dialogService)
    {
        _templateLoader = templateLoader;
        _dialogService = dialogService;

        RecentTemplates = new ObservableCollection<RecentTemplate>();
        ServerTemplates = new ObservableCollection<TemplateSummary>();

        BrowseFileCommand = new RelayCommand(_ => ExecuteBrowseFile());
        LoadRecentCommand = new RelayCommand<RecentTemplate>(ExecuteLoadRecent);
        LoadFromServerCommand = new RelayCommand<TemplateSummary>(ExecuteLoadFromServer);
        CancelCommand = new RelayCommand(_ => RequestClose?.Invoke(false));

        LoadRecentItems();
    }

    public ObservableCollection<RecentTemplate> RecentTemplates { get; }
    public ObservableCollection<TemplateSummary> ServerTemplates { get; }

    public ICommand BrowseFileCommand { get; }
    public ICommand LoadRecentCommand { get; }
    public ICommand LoadFromServerCommand { get; }
    public ICommand CancelCommand { get; }

    public event Action<ExternalTemplateDefinition>? TemplateSelected;
    public event Action<bool>? RequestClose;

    private void LoadRecentItems()
    {
        // TODO: 从配置文件加载最近使用的模板
        // 临时添加示例数据
    }

    private void ExecuteBrowseFile()
    {
        var filePath = _dialogService.OpenFile(
            "JSON 文件 (*.json)|*.json|所有文件 (*.*)|*.*",
            "选择模板文件");

        if (!string.IsNullOrEmpty(filePath))
        {
            LoadTemplateFromFile(filePath);
        }
    }

    private void ExecuteLoadRecent(RecentTemplate? template)
    {
        if (template != null)
        {
            LoadTemplateFromFile(template.FilePath);
        }
    }

    private void ExecuteLoadFromServer(TemplateSummary? template)
    {
        if (template != null)
        {
            // TODO: 从服务器加载模板
            _dialogService.ShowInfo("服务器加载功能开发中", "提示");
        }
    }

    private void LoadTemplateFromFile(string filePath)
    {
        try
        {
            var template = _templateLoader.LoadFromFile(filePath);
            TemplateSelected?.Invoke(template);
            RequestClose?.Invoke(true);
        }
        catch (Exception ex)
        {
            _dialogService.ShowError($"加载模板失败: {ex.Message}");
        }
    }
}

public class RecentTemplate
{
    public string Name { get; set; } = string.Empty;
    public string FilePath { get; set; } = string.Empty;
    public DateTime LastUsed { get; set; }
}

public class TemplateSummary
{
    public Guid Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string Type { get; set; } = string.Empty;
    public int Version { get; set; }
}
```

#### Step 3.6：创建 SidePanelViewModel

**新建文件**：`ViewModels/SidePanelViewModel.cs`

```csharp
using System.Windows.Input;
using ReportDataMaker.Infrastructure;

namespace ReportDataMaker.ViewModels;

public class SidePanelViewModel : ViewModelBase
{
    private bool _isExpanded = true;
    public bool IsExpanded
    {
        get => _isExpanded;
        set => SetProperty(ref _isExpanded, value);
    }

    public ICommand ToggleCommand { get; }

    public SidePanelViewModel()
    {
        ToggleCommand = new RelayCommand(_ => IsExpanded = !IsExpanded);
    }
}
```

#### Step 3.7：创建 AddAdapterDialogViewModel

**新建文件**：`ViewModels/Dialogs/AddAdapterDialogViewModel.cs`

```csharp
using System.Windows.Input;
using ReportDataMaker.Infrastructure;
using Xinglin.ReportEditor.Contracts.Enums;

namespace ReportDataMaker.ViewModels.Dialogs;

public class AddAdapterDialogViewModel : ViewModelBase
{
    private string _adapterName = "新适配器";
    public string AdapterName
    {
        get => _adapterName;
        set => SetProperty(ref _adapterName, value);
    }

    private AdapterType? _selectedAdapterType;
    public AdapterType? SelectedAdapterType
    {
        get => _selectedAdapterType;
        set => SetProperty(ref _selectedAdapterType, value);
    }

    public IEnumerable<AdapterTypeItem> AvailableAdapterTypes { get; } = new[]
    {
        new AdapterTypeItem { Type = AdapterType.Excel, Name = "Excel 导入", Description = "从 Excel 文件导入数据" },
        new AdapterTypeItem { Type = AdapterType.Database, Name = "数据库查询", Description = "从数据库查询数据" },
        new AdapterTypeItem { Type = AdapterType.Api, Name = "API 调用", Description = "从 HTTP API 获取数据", IsEnabled = false }
    };

    public ICommand ConfirmCommand { get; }
    public ICommand CancelCommand { get; }

    public event Action<bool>? RequestClose;

    public AddAdapterDialogViewModel()
    {
        ConfirmCommand = new RelayCommand(_ =>
        {
            if (SelectedAdapterType != null && !string.IsNullOrWhiteSpace(AdapterName))
            {
                RequestClose?.Invoke(true);
            }
        });
        CancelCommand = new RelayCommand(_ => RequestClose?.Invoke(false));
    }
}

public class AdapterTypeItem
{
    public AdapterType Type { get; set; }
    public string Name { get; set; } = string.Empty;
    public string Description { get; set; } = string.Empty;
    public bool IsEnabled { get; set; } = true;
}
```

### 阶段四：服务接口定义（Day 9）

#### Step 4.1：创建服务接口

**新建文件**：`Services/ITemplateLoaderService.cs`

```csharp
using ReportDataMaker.Models;

namespace ReportDataMaker.Services;

public interface ITemplateLoaderService
{
    ExternalTemplateDefinition LoadFromFile(string filePath);
    ExternalTemplateDefinition LoadFromJson(string json);
    Task<ExternalTemplateDefinition> LoadFromServerAsync(Guid templateId);
}
```

**新建文件**：`Services/ITemplatePreviewService.cs`

```csharp
using System.Windows.Media;
using ReportDataMaker.Models;

namespace ReportDataMaker.Services;

public interface ITemplatePreviewService
{
    Visual GeneratePreview(ExternalTemplateDefinition template);
    byte[] RenderToImage(ExternalTemplateDefinition template, double dpi = 96);
}
```

**新建文件**：`Services/IDataBindingService.cs`

```csharp
using ReportDataMaker.Models;

namespace ReportDataMaker.Services;

public interface IDataBindingService
{
    void ApplyData(ExternalTemplateDefinition template, Dictionary<string, object> data);
    Dictionary<string, object> ExtractData(ExternalTemplateDefinition template);
}
```

#### Step 4.2：实现服务（使用现有代码迁移）

**新建文件**：`Services/TemplateLoaderService.cs`

```csharp
using System.IO;
using Newtonsoft.Json;
using ReportDataMaker.Infrastructure;
using ReportDataMaker.Models;
using Xinglin.ReportEditor.Contracts.Converters;

namespace ReportDataMaker.Services;

public class TemplateLoaderService : ITemplateLoaderService
{
    private readonly IDialogService _dialogService;

    public TemplateLoaderService(IDialogService dialogService)
    {
        _dialogService = dialogService;
    }

    public ExternalTemplateDefinition LoadFromFile(string filePath)
    {
        if (!File.Exists(filePath))
            throw new FileNotFoundException("模板文件不存在", filePath);

        var json = File.ReadAllText(filePath);
        return LoadFromJson(json);
    }

    public ExternalTemplateDefinition LoadFromJson(string json)
    {
        var template = JsonConvert.DeserializeObject<ExternalTemplateDefinition>(json, 
            new JsonSerializerSettings
            {
                NullValueHandling = NullValueHandling.Ignore,
                Converters = new[] { new ElementJsonConverter() }
            });

        if (template == null)
            throw new InvalidOperationException("无法解析模板 JSON");

        return template;
    }

    public Task<ExternalTemplateDefinition> LoadFromServerAsync(Guid templateId)
    {
        // TODO: 实现服务器加载
        throw new NotImplementedException();
    }
}
```

**新建文件**：`Services/TemplatePreviewService.cs`

```csharp
using System.Windows;
using System.Windows.Media;
using ReportDataMaker.Models;

namespace ReportDataMaker.Services;

public class TemplatePreviewService : ITemplatePreviewService
{
    public Visual GeneratePreview(ExternalTemplateDefinition template)
    {
        // TODO: 实现预览生成
        var canvas = new Canvas { Width = 800, Height = 600, Background = Brushes.White };
        return canvas;
    }

    public byte[] RenderToImage(ExternalTemplateDefinition template, double dpi = 96)
    {
        // TODO: 实现图像渲染
        return Array.Empty<byte>();
    }
}
```

**新建文件**：`Services/DataBindingService.cs`

```csharp
using ReportDataMaker.Models;

namespace ReportDataMaker.Services;

public class DataBindingService : IDataBindingService
{
    public void ApplyData(ExternalTemplateDefinition template, Dictionary<string, object> data)
    {
        if (template.Elements == null) return;

        foreach (var element in template.Elements)
        {
            if (!string.IsNullOrEmpty(element.DataPath) && data.TryGetValue(element.DataPath, out var value))
            {
                element.DefaultValue = value?.ToString() ?? string.Empty;
            }
        }
    }

    public Dictionary<string, object> ExtractData(ExternalTemplateDefinition template)
    {
        var data = new Dictionary<string, object>();

        if (template.Elements == null) return data;

        foreach (var element in template.Elements)
        {
            if (!string.IsNullOrEmpty(element.DataPath))
            {
                data[element.DataPath] = element.DefaultValue ?? string.Empty;
            }
        }

        return data;
    }
}

public class AdapterConfigBase
{
    public string AdapterId { get; set; } = string.Empty;
    public AdapterType Type { get; set; }
    public string DisplayName { get; set; } = string.Empty;
    public bool IsEnabled { get; set; } = true;
}
```

**新建文件**：`Services/AdapterConfigStore.cs`

```csharp
using System.IO;
using System.Text.Json;
using Xinglin.ReportEditor.Contracts.Enums;

namespace ReportDataMaker.Services;

public class AdapterConfigStore
{
    private readonly string _configPath;

    public AdapterConfigStore()
    {
        var appDataPath = Path.Combine(
            Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
            "ReportDataMaker");

        Directory.CreateDirectory(appDataPath);
        _configPath = Path.Combine(appDataPath, "adapters.json");
    }

    public List<AdapterConfigBase> Load(string templateName)
    {
        if (!File.Exists(_configPath))
            return new List<AdapterConfigBase>();

        try
        {
            var json = File.ReadAllText(_configPath);
            var configs = JsonSerializer.Deserialize<Dictionary<string, List<AdapterConfigBase>>>(json);
            return configs?.GetValueOrDefault(templateName, new List<AdapterConfigBase>()) 
                ?? new List<AdapterConfigBase>();
        }
        catch
        {
            return new List<AdapterConfigBase>();
        }
    }

    public void Save(string templateName, List<AdapterConfigBase> configs)
    {
        Dictionary<string, List<AdapterConfigBase>> allConfigs;

        if (File.Exists(_configPath))
        {
            try
            {
                var json = File.ReadAllText(_configPath);
                allConfigs = JsonSerializer.Deserialize<Dictionary<string, List<AdapterConfigBase>>>(json) 
                    ?? new Dictionary<string, List<AdapterConfigBase>>();
            }
            catch
            {
                allConfigs = new Dictionary<string, List<AdapterConfigBase>>();
            }
        }
        else
        {
            allConfigs = new Dictionary<string, List<AdapterConfigBase>>();
        }

        allConfigs[templateName] = configs;
        var outputJson = JsonSerializer.Serialize(allConfigs, new JsonSerializerOptions { WriteIndented = true });
        File.WriteAllText(_configPath, outputJson);
    }
}
```

### 阶段五：Tab 页视图实现（Day 10-12）

#### Step 5.1：创建 DataEntryTab

**新建文件**：`Views/Tabs/DataEntryTab.xaml`

```xml
<UserControl x:Class="ReportDataMaker.Views.Tabs.DataEntryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="从适配器填充" 
                    Style="{StaticResource ButtonPrimary}"
                    Margin="0,0,8,0"/>
            <Button Content="清空所有" 
                    Style="{StaticResource ButtonDefault}"/>
        </StackPanel>

        <!-- 字段列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Fields}" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <hc:UniformSpacingPanel Spacing="16" 
                                               Orientation="Vertical"
                                               Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" 
                                           FontWeight="Bold" 
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding DataPath}" 
                                           Foreground="Gray" 
                                           FontSize="11"
                                           Margin="0,0,0,4"/>
                                <hc:TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                           hc:InfoElement.Placeholder="请输入..."
                                           MinWidth="300"/>
                            </StackPanel>
                        </hc:UniformSpacingPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
```

**新建文件**：`Views/Tabs/DataEntryTab.xaml.cs`

```csharp
using System.Windows.Controls;

namespace ReportDataMaker.Views.Tabs;

public partial class DataEntryTab : UserControl
{
    public DataEntryTab()
    {
        InitializeComponent();
    }
}
```

#### Step 5.2：创建 PreviewTab

**新建文件**：`Views/Tabs/PreviewTab.xaml`

```xml
<UserControl x:Class="ReportDataMaker.Views.Tabs.PreviewTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="刷新预览" 
                    Style="{StaticResource ButtonPrimary}"
                    Command="{Binding RefreshCommand}"
                    Margin="0,0,8,0"/>
            <hc:NumericUpDown Value="{Binding ZoomLevel}" 
                             Minimum="25" Maximum="400" 
                             Increment="25"
                             Width="120"/>
            <TextBlock Text="%" VerticalAlignment="Center" Margin="4,0,0,0"/>
        </StackPanel>

        <!-- 预览区域 -->
        <Border Grid.Row="1" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1"
                Background="#FAFAFA">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         Padding="16">
                <Border Background="White" 
                        BorderBrush="#CCCCCC" 
                        BorderThickness="1"
                        Padding="8"
                        HorizontalAlignment="Center">
                    <!-- TODO: 插入 PreviewVisual -->
                    <TextBlock Text="预览区域" 
                               Foreground="Gray"
                               FontSize="18"
                               HorizontalAlignment="Center"
                               Margin="100"/>
                </Border>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
```

**新建文件**：`Views/Tabs/PreviewTab.xaml.cs`

```csharp
using System.Windows.Controls;

namespace ReportDataMaker.Views.Tabs;

public partial class PreviewTab : UserControl
{
    public PreviewTab()
    {
        InitializeComponent();
    }
}
```

### 阶段六：对话框视图实现（Day 13-14）

#### Step 6.1：创建 TemplateLoadDialog

**新建文件**：`Views/Dialogs/TemplateLoadDialog.xaml`

```xml
<hc:Window x:Class="ReportDataMaker.Views.Dialogs.TemplateLoadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="加载模板"
        Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowTitle="True">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标签选择 -->
        <hc:TabControl Grid.Row="1" Style="{StaticResource TabControlCapsule}">
            
            <!-- 最近使用 -->
            <TabItem Header="最近使用">
                <ListBox ItemsSource="{Binding RecentTemplates}"
                         SelectedItem="{Binding SelectedRecent}"
                         Margin="8">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding LastUsed, StringFormat=' ({0:yyyy-MM-dd})'}" 
                                           Foreground="Gray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>

            <!-- 浏览文件 -->
            <TabItem Header="浏览文件">
                <StackPanel Margin="16">
                    <Button Content="选择文件..."
                            Command="{Binding BrowseFileCommand}"
                            HorizontalAlignment="Left"
                            Style="{StaticResource ButtonPrimary}"/>
                    <TextBlock Text="选择一个 .json 格式的模板文件" 
                               Foreground="Gray" 
                               Margin="0,8,0,0"/>
                </StackPanel>
            </TabItem>

            <!-- 从服务器 -->
            <TabItem Header="从服务器">
                <ListBox ItemsSource="{Binding ServerTemplates}"
                         Margin="8">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Foreground="Gray" FontSize="11">
                                    <Run Text="{Binding Type}"/>
                                    <Run Text=" - v"/>
                                    <Run Text="{Binding Version}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </hc:TabControl>

        <!-- 按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="取消" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ButtonDefault}"
                    Width="80"
                    Margin="0,0,8,0"/>
            <Button Content="加载" 
                    Style="{StaticResource ButtonPrimary}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</hc:Window>
```

**新建文件**：`Views/Dialogs/TemplateLoadDialog.xaml.cs`

```csharp
using ReportDataMaker.ViewModels;

namespace ReportDataMaker.Views.Dialogs;

public partial class TemplateLoadDialog : HandyControl.Controls.Window
{
    public TemplateLoadDialog()
    {
        InitializeComponent();
    }
}
```

#### Step 6.2：创建 AddAdapterDialog

**新建文件**：`Views/Dialogs/AddAdapterDialog.xaml`

```xml
<hc:Window x:Class="ReportDataMaker.Views.Dialogs.AddAdapterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="添加适配器"
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 适配器名称 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="适配器名称" FontWeight="Bold" Margin="0,0,0,4"/>
            <hc:TextBox Text="{Binding AdapterName, UpdateSourceTrigger=PropertyChanged}"
                       hc:InfoElement.Placeholder="请输入适配器名称"/>
        </StackPanel>

        <!-- 适配器类型选择 -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="适配器类型" FontWeight="Bold" Margin="0,0,0,8"/>
            <ItemsControl ItemsSource="{Binding AvailableAdapterTypes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton GroupName="AdapterType"
                                     IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                     IsEnabled="{Binding IsEnabled}"
                                     Margin="0,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Description, StringFormat=' - {0}'}" 
                                           Foreground="Gray"/>
                                <TextBlock Text=" (开发中)" 
                                           Foreground="Orange"
                                           Visibility="{Binding IsEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </RadioButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- 按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="取消" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ButtonDefault}"
                    Width="80"
                    Margin="0,0,8,0"/>
            <Button Content="确定" 
                    Command="{Binding ConfirmCommand}"
                    Style="{StaticResource ButtonPrimary}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</hc:Window>
```

**新建文件**：`Views/Dialogs/AddAdapterDialog.xaml.cs`

```csharp
using ReportDataMaker.ViewModels.Dialogs;

namespace ReportDataMaker.Views.Dialogs;

public partial class AddAdapterDialog : HandyControl.Controls.Window
{
    public AddAdapterDialog()
    {
        InitializeComponent();
    }
}
```

### 阶段七：值转换器（Day 15）

#### Step 7.1：创建转换器

**新建文件**：`Converters/BoolToVisibilityConverter.cs`

```csharp
using System.Globalization;
using System.Windows;
using System.Windows.Data;

namespace ReportDataMaker.Converters;

public class BoolToVisibilityConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is bool boolValue)
        {
            return boolValue ? Visibility.Visible : Visibility.Collapsed;
        }
        return Visibility.Collapsed;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is Visibility visibility)
        {
            return visibility == Visibility.Visible;
        }
        return false;
    }
}

public class InverseBoolToVisibilityConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is bool boolValue)
        {
            return boolValue ? Visibility.Collapsed : Visibility.Visible;
        }
        return Visibility.Visible;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is Visibility visibility)
        {
            return visibility != Visibility.Visible;
        }
        return true;
    }
}

public class BoolToWidthConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is bool boolValue && parameter is string widths)
        {
            var parts = widths.Split(',');
            if (parts.Length == 2)
            {
                return boolValue ? double.Parse(parts[0]) : double.Parse(parts[1]);
            }
        }
        return 240.0;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        throw new NotImplementedException();
    }
}
```

---

## 5. 文件变更清单

### 新建文件

| 文件路径 | 说明 |
|---------|------|
| `Infrastructure/ViewModelBase.cs` | ViewModel 基类 |
| `Infrastructure/RelayCommand.cs` | 同步命令 |
| `Infrastructure/AsyncRelayCommand.cs` | 异步命令 |
| `Infrastructure/IDialogService.cs` | 对话框服务接口 |
| `Infrastructure/DialogService.cs` | 对话框服务实现 |
| `Infrastructure/ServiceLocator.cs` | 服务定位器 |
| `Services/ITemplateLoaderService.cs` | 模板加载服务接口 |
| `Services/ITemplatePreviewService.cs` | 预览服务接口 |
| `Services/IDataBindingService.cs` | 数据绑定服务接口 |
| `Services/TemplateLoaderService.cs` | 模板加载服务实现 |
| `Services/TemplatePreviewService.cs` | 预览服务实现 |
| `Services/DataBindingService.cs` | 数据绑定服务实现 |
| `Services/AdapterConfigStore.cs` | 适配器配置存储 |
| `ViewModels/MainViewModel.cs` | 主视图模型 |
| `ViewModels/TemplateLoadViewModel.cs` | 模板加载视图模型 |
| `ViewModels/SidePanelViewModel.cs` | 侧边栏视图模型 |
| `ViewModels/Tabs/TabViewModelBase.cs` | Tab 基类 |
| `ViewModels/Tabs/DataEntryTabViewModel.cs` | 数据录入 Tab 视图模型 |
| `ViewModels/Tabs/PreviewTabViewModel.cs` | 预览 Tab 视图模型 |
| `ViewModels/Dialogs/AddAdapterDialogViewModel.cs` | 添加适配器对话框视图模型 |
| `Views/MainWindow.xaml` | 主窗口 |
| `Views/MainWindow.xaml.cs` | 主窗口代码隐藏 |
| `Views/Tabs/DataEntryTab.xaml` | 数据录入 Tab |
| `Views/Tabs/DataEntryTab.xaml.cs` | |
| `Views/Tabs/PreviewTab.xaml` | 预览 Tab |
| `Views/Tabs/PreviewTab.xaml.cs` | |
| `Views/Dialogs/TemplateLoadDialog.xaml` | 模板加载对话框 |
| `Views/Dialogs/TemplateLoadDialog.xaml.cs` | |
| `Views/Dialogs/AddAdapterDialog.xaml` | 添加适配器对话框 |
| `Views/Dialogs/AddAdapterDialog.xaml.cs` | |
| `Converters/BoolToVisibilityConverter.cs` | 值转换器 |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `App.xaml` | 添加 HandyControl 主题资源 |
| `App.xaml.cs` | 添加 DI 容器初始化 |
| `ReportDataMaker.csproj` | 添加 HandyControl 和 DI NuGet 引用 |

### 删除文件（可选，保留向后兼容）

| 文件路径 | 说明 |
|---------|------|
| `MainWindow_old.xaml` | 旧主窗口（如存在） |
| `DataEntryWindow.xaml` | 旧数据录入窗口（迁移到 Tab） |

---

## 6. 验收标准

### 6.1 编译通过 ⚠️ 待验证

- [ ] `dotnet build` 无错误
- [ ] 无警告（Warning）

### 6.2 功能验证 ⚠️ 待验证

- [ ] 启动显示模板加载对话框
- [ ] 从文件浏览加载模板成功
- [ ] 模板加载后显示主界面
- [ ] 左侧面板可折叠/展开
- [ ] TabControl 显示数据录入和预览 Tab
- [ ] 添加适配器对话框正常工作
- [ ] 菜单栏命令正常响应

### 6.3 UI 验证 ⚠️ 待验证

- [ ] HandyControl 主题正常应用
- [ ] 布局响应窗口大小变化
- [ ] 状态栏显示正确信息

### 6.4 架构验证 ⚠️ 待验证

- [ ] ViewModel 不直接引用 View
- [ ] 服务通过 DI 注入
- [ ] 命令正确使用 RelayCommand/AsyncRelayCommand

---

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| HandyControl 版本兼容性 | 运行时错误 | 使用稳定的 3.5.1 版本 |
| 现有代码迁移遗漏 | 功能缺失 | 全面测试现有功能 |
| Tab 关闭后数据丢失 | 用户体验差 | 实现自动保存机制 |

---

## 8. 后续版本依赖

```
v0.4.0 (WPF MVVM 重构)
    │
    ├── v0.5.0 (Excel 适配器) - 复用 FieldMapping, DataPreview Components
    │
    └── v0.6.0 (数据库适配器) - 复用 FieldMapping, DataPreview Components
```

---

*本文档为 v0.4.0 WPF MVVM 重构详细设计和开发计划*
