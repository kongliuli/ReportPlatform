# v0.3.0 架构统一 - 详细设计和开发计划

> 文档版本：1.0.0  
> 创建日期：2026-05-08  
> 预计工期：2 周  
> 状态：规划中

---

## 1. 概述

### 1.1 目标

将分散在 ReportDataMaker 项目中的模型定义和接口统一到 Contracts 项目，建立清晰的依赖边界，为后续适配器开发和 WPF MVVM 重构奠定基础。

### 1.2 核心价值

- **单一数据源**：Contracts 成为唯一的模型定义来源，消除多处定义导致的不一致
- **依赖清晰**：各层严格依赖 Contracts，无循环依赖
- **向后兼容**：保留双格式支持，确保现有模板和代码继续工作
- **可扩展性**：新增元素类型或适配器时，只需在 Contracts 中添加定义

---

## 2. 当前状态分析

### 2.1 已有的模型定义位置

| 模型 | 当前位置 | 问题 |
|------|---------|------|
| ExternalElementBase 体系 | ReportDataMaker/Models | 应移入 Contracts |
| ExternalTemplateDefinition | ReportDataMaker/Models | 应移入 Contracts |
| IDataAdapter 接口 | ReportDataMaker/Models/IDataAdapter.cs | 应移入 Contracts |
| AdapterType 枚举 | ReportDataMaker/Services | 分散定义，应统一 |
| DataBindingDefinition | 缺失 | 应在 Contracts 中新增 |
| ElementGroup 枚举 | 缺失 | 应新增 |

### 2.2 目标状态

```
Contracts/                          # 唯一的模型定义来源
├── Enums/
│   ├── ElementGroup.cs            # 四分体系：Fixed/Context/Editable/DataAdapter
│   ├── ElementAdaptationGroup.cs   # 适配分组：基础/表单/数据/高级
│   └── AdapterType.cs             # 适配器类型：Context/Excel/Database/Api
│
├── Models/
│   ├── Elements/
│   │   ├── ElementBase.cs         # 抽象基类
│   │   ├── ExternalElementBase.cs # 元素基类
│   │   ├── TextElement.cs
│   │   ├── LineElement.cs
│   │   ├── TableElement.cs
│   │   └── ... (23 种元素)
│   │
│   ├── Template/
│   │   ├── TemplateDefinition.cs
│   │   ├── PageSettings.cs
│   │   └── DataBindingDefinition.cs
│   │
│   └── Adapters/
│       ├── IDataAdapter.cs
│       ├── AdapterConfigBase.cs
│       ├── AdapterResult.cs
│       └── FieldSchema.cs
│
├── Converters/
│   └── ElementJsonConverter.cs    # 双格式 $type 转换器
│
└── Contracts.csproj

ReportDataMaker/                   # 仅保留使用逻辑
├── Models/                        # 仅引用 Contracts
├── Services/
└── ViewModels/
```

### 2.3 元素适配分组设计

基于 `docs/architecture/element-adaptation-groups.md` 的分析，23 种元素按适配特性分为四组：

```csharp
public enum ElementGroup
{
    Fixed,       // 固定内容，无需适配
    Context,     // 上下文自动填充
    Editable,    // 手动编辑
    DataAdapter  // 外部数据适配
}
```

23 种元素的适配分组映射：

| 元素类型 | Group | 适配说明 |
|---------|-------|---------|
| LineElement | Fixed | 固定线条，无数据 |
| DividerElement | Fixed | 固定分隔线 |
| TextElement (无DataPath) | Fixed | 固定文本 |
| ShapeElement | Fixed | 固定形状 |
| ContainerElement | Fixed | 固定容器 |
| HeaderElement | Fixed | 固定页眉 |
| FooterElement | Fixed | 固定页脚 |
| WatermarkElement | Fixed | 固定水印 |
| IconElement | Fixed | 固定图标 |
| TextElement (有DataPath) | Context/Editable | 根据 DataPath 前缀判断 |
| ImageElement | Context/Editable | 根据 DataPath 前缀判断 |
| NumberElement | Editable | 手动输入数字 |
| DateElement | Editable | 手动输入日期 |
| DropdownElement | Editable | 手动选择 |
| CheckboxElement | Editable | 手动勾选 |
| RadioElement | Editable | 手动选择 |
| BarcodeElement | Editable | 手动输入后生成 |
| QrCodeElement | Editable | 手动输入后生成 |
| SignatureElement | Editable | 手动签名 |
| HyperlinkElement | Editable | 手动输入链接 |
| PageNumberElement | Context | 自动页码 |
| TableElement | DataAdapter | 通常由外部数据填充 |
| RepeatElement | DataAdapter | 通常由外部数据填充 |
| ChartElement | DataAdapter | 通常由外部数据填充 |

---

## 3. 详细实施步骤

### 阶段一：模型层统一（Day 1-3）

#### Step 1.1：创建 Contracts/Enums 目录和枚举定义

**新建文件**：`Contracts/Enums/ElementGroup.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Enums;

public enum ElementGroup
{
    Fixed,
    Context,
    Editable,
    DataAdapter
}
```

**新建文件**：`Contracts/Enums/ElementAdaptationGroup.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Enums;

public enum ElementAdaptationGroup
{
    Basic,      // Line, Divider, Shape - 仅展示
    Form,       // Text, Number, Date, Dropdown, Checkbox, Radio - 表单输入
    Data,       // Table, Repeat, Chart - 数据驱动
    Advanced    // Barcode, QrCode, Signature, Image, Hyperlink - 特殊功能
}
```

**新建文件**：`Contracts/Enums/AdapterType.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Enums;

public enum AdapterType
{
    Context,    // 上下文配置适配器
    Excel,      // Excel 导入适配器
    Database,   // 数据库查询适配器
    Api         // API 调用适配器
}
```

#### Step 1.2：创建元素基类和23种元素模型

**新建文件**：`Contracts/Models/Elements/ElementBase.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Models.Elements;

public abstract class ElementBase
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public double X { get; set; }
    public double Y { get; set; }
    public double Width { get; set; }
    public double Height { get; set; }
    public double Rotation { get; set; }
    public int ZIndex { get; set; }
    public string? Tooltip { get; set; }
    public bool IsLocked { get; set; }
}
```

**新建文件**：`Contracts/Models/Elements/ExternalElementBase.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;

namespace Xinglin.ReportEditor.Contracts.Models.Elements;

public abstract class ExternalElementBase : ElementBase
{
    public string? Label { get; set; }
    public string? DataPath { get; set; }
    public bool IsDataBound => !string.IsNullOrEmpty(DataPath);
    public bool IsRequired { get; set; }
    public ElementGroup Group { get; set; } = ElementGroup.Fixed;
    public string? AdapterId { get; set; }
}
```

**元素模型文件清单**（23种）：

| 文件名 | 继承 | 关键属性 |
|--------|------|---------|
| LineElement.cs | ElementBase | StrokeColor, StrokeWidth, X1, Y1, X2, Y2 |
| TextElement.cs | ExternalElementBase | Text, FontSize, FontWeight, TextColor, TextAlign |
| ImageElement.cs | ExternalElementBase | Src, Fit, MaintainAspectRatio, AltText |
| TableElement.cs | ExternalElementBase | Rows, Cols, CellDefinitions, HeaderRows |
| NumberElement.cs | ExternalElementBase | Value, MinValue, MaxValue, DecimalPlaces, Unit |
| DateElement.cs | ExternalElementBase | Value, Format, MinDate, MaxDate |
| DropdownElement.cs | ExternalElementBase | Options, SelectedValue, IsMultiSelect |
| CheckboxElement.cs | ExternalElementBase | Checked, CheckColor |
| RadioElement.cs | ExternalElementBase | GroupName, Value, IsChecked |
| BarcodeElement.cs | ExternalElementBase | Value, Format, ShowText, LineColor |
| QrCodeElement.cs | ExternalElementBase | Value, ErrorCorrectionLevel, Margin, Color |
| SignatureElement.cs | ExternalElementBase | Placeholder, LineColor, LineWidth |
| ShapeElement.cs | ElementBase | ShapeType, FillColor, StrokeColor, StrokeWidth |
| ContainerElement.cs | ElementBase | Children, Layout, Padding, ClipContent |
| RepeatElement.cs | ExternalElementBase | DataSource, ItemTemplate, Direction, Gap |
| HeaderElement.cs | ElementBase | Children, ShowOnFirstPage, ShowOnAllPages |
| FooterElement.cs | ElementBase | Children, ShowOnLastPage, ShowOnAllPages |
| PageNumberElement.cs | ElementBase | Format, StartPage |
| WatermarkElement.cs | ElementBase | Text, Angle, Color, Repeat, Opacity |
| IconElement.cs | ElementBase | IconName, IconSet, Color, Size |
| HyperlinkElement.cs | ExternalElementBase | Url, OpenInNewTab, Text |
| DividerElement.cs | ElementBase | Thickness, Color, Style |
| ChartElement.cs | ExternalElementBase | ChartType, Title, DataSource, ShowLegend, ShowGrid |

#### Step 1.3：创建模板定义模型

**新建文件**：`Contracts/Models/Template/TemplateDefinition.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Models.Template;

public class TemplateDefinition
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string Name { get; set; } = string.Empty;
    public int Version { get; set; } = 1;
    public string Type { get; set; } = string.Empty;
    public string? HospitalId { get; set; }
    public PageSettings PageSettings { get; set; } = new();
    public List<DataBindingDefinition> DataBindings { get; set; } = new();
    public List<ExternalElementBase> Elements { get; set; } = new();
}
```

**新建文件**：`Contracts/Models/Template/PageSettings.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Models.Template;

public class PageSettings
{
    public double PageWidth { get; set; } = 210;      // mm (A4)
    public double PageHeight { get; set; } = 297;     // mm (A4)
    public double MarginLeft { get; set; } = 20;
    public double MarginRight { get; set; } = 20;
    public double MarginTop { get; set; } = 20;
    public double MarginBottom { get; set; } = 20;
    public PageOrientation Orientation { get; set; } = PageOrientation.Portrait;
    public string BackgroundColor { get; set; } = "#FFFFFF";
}

public enum PageOrientation { Portrait, Landscape }
```

**新建文件**：`Contracts/Models/Template/DataBindingDefinition.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Models.Template;

public class DataBindingDefinition
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string ElementId { get; set; } = string.Empty;
    public string DataPath { get; set; } = string.Empty;
    public BindingType BindingType { get; set; } = BindingType.Text;
    public string? FormatString { get; set; }
    public string? DefaultValue { get; set; }
    public string? Transform { get; set; }
}

public enum BindingType
{
    Text,
    Image,
    Visibility,
    Repeat,
    Style
}
```

#### Step 1.4：创建适配器相关模型

**新建文件**：`Contracts/Models/Adapters/IDataAdapter.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;

namespace Xinglin.ReportEditor.Contracts.Models.Adapters;

public interface IDataAdapter
{
    string AdapterId { get; }
    string AdapterName { get; }
    AdapterType Type { get; }
    IReadOnlyList<string> TargetDataPaths { get; }
    
    Task<AdapterResult> ReadDataAsync();
    Task<AdapterResult> ReadBatchDataAsync();
    Task<ValidationResult> ValidateConfigAsync();
}
```

**新建文件**：`Contracts/Models/Adapters/AdapterResult.cs`

```csharp
namespace Xinglin.ReportEditor.Contracts.Models.Adapters;

public class AdapterResult
{
    public bool Success { get; set; }
    public string? ErrorMessage { get; set; }
    public Dictionary<string, object> Data { get; set; } = new();
    public List<Dictionary<string, object>> BatchData { get; set; } = new();
}

public class ValidationResult
{
    public bool IsValid { get; set; }
    public List<string> Errors { get; set; } = new();
    public List<string> Warnings { get; set; } = new();
    
    public static ValidationResult Success => new() { IsValid = true };
    public static ValidationResult Fail(params string[] errors) => new() { IsValid = false, Errors = errors.ToList() };
}
```

**新建文件**：`Contracts/Models/Adapters/AdapterConfigBase.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;

namespace Xinglin.ReportEditor.Contracts.Models.Adapters;

public abstract class AdapterConfigBase
{
    public string AdapterId { get; set; } = Guid.NewGuid().ToString();
    public AdapterType Type { get; set; }
    public string DisplayName { get; set; } = string.Empty;
    public Dictionary<string, string> FieldMappings { get; set; } = new();
    public bool IsEnabled { get; set; } = true;
}
```

**新建文件**：`Contracts/Models/Adapters/FieldSchema.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;

namespace Xinglin.ReportEditor.Contracts.Models.Adapters;

public class FieldSchema
{
    public string DataPath { get; set; } = string.Empty;
    public string Label { get; set; } = string.Empty;
    public FieldDataType DataType { get; set; } = FieldDataType.Text;
    public string? Format { get; set; }
    public List<string>? Options { get; set; }
    public bool IsRequired { get; set; }
    public double? MinValue { get; set; }
    public double? MaxValue { get; set; }
    public int? DecimalPlaces { get; set; }
}

public enum FieldDataType
{
    Text,
    Number,
    Date,
    Dropdown,
    Boolean
}
```

### 阶段二：转换器统一（Day 4-5）

#### Step 2.1：创建双格式 JSON 转换器

**新建文件**：`Contracts/Converters/ElementJsonConverter.cs`

```csharp
using System.Globalization;
using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using Xinglin.ReportEditor.Contracts.Enums;
using Xinglin.ReportEditor.Contracts.Models.Elements;
using Xinglin.ReportEditor.Contracts.Models.Template;

namespace Xinglin.ReportEditor.Contracts.Converters;

public class ElementJsonConverter : JsonConverter<ExternalElementBase>
{
    public static readonly Dictionary<string, Type> WebShortTypeMap = new()
    {
        ["text"] = typeof(TextElement),
        ["line"] = typeof(LineElement),
        ["table"] = typeof(TableElement),
        ["number"] = typeof(NumberElement),
        ["date"] = typeof(DateElement),
        ["dropdown"] = typeof(DropdownElement),
        ["checkbox"] = typeof(CheckboxElement),
        ["radio"] = typeof(RadioElement),
        ["image"] = typeof(ImageElement),
        ["shape"] = typeof(ShapeElement),
        ["divider"] = typeof(DividerElement),
        ["barcode"] = typeof(BarcodeElement),
        ["qrcode"] = typeof(QrCodeElement),
        ["signature"] = typeof(SignatureElement),
        ["container"] = typeof(ContainerElement),
        ["repeat"] = typeof(RepeatElement),
        ["header"] = typeof(HeaderElement),
        ["footer"] = typeof(FooterElement),
        ["pageNumber"] = typeof(PageNumberElement),
        ["watermark"] = typeof(WatermarkElement),
        ["icon"] = typeof(IconElement),
        ["hyperlink"] = typeof(HyperlinkElement),
        ["chart"] = typeof(ChartElement)
    };

    public static readonly Dictionary<Type, string> ReverseTypeMap;

    static ElementJsonConverter()
    {
        ReverseTypeMap = WebShortTypeMap.ToDictionary(kvp => kvp.Value, kvp => kvp.Key);
    }

    public override ExternalElementBase? ReadJson(JsonReader reader, Type objectType, 
        ExternalElementBase? existingValue, bool hasExistingValue, JsonSerializer serializer)
    {
        var jsonObject = JObject.Load(reader);
        var typeString = jsonObject["$type"]?.ToString() 
                      ?? jsonObject["Type"]?.ToString();

        var elementType = MapType(typeString);
        var element = (ExternalElementBase?)jsonObject.ToObject(elementType, serializer);
        
        if (element != null)
        {
            element.Group = ClassifyElement(element);
        }
        
        return element;
    }

    public override void WriteJson(JsonWriter writer, ExternalElementBase? value, JsonSerializer serializer)
    {
        var jObject = JObject.FromObject(value, serializer);
        var shortType = ReverseTypeMap.GetValueOrDefault(value.GetType(), "unknown");
        jObject["$type"] = $"template.element.{shortType}";
        jObject.WriteTo(writer);
    }

    private static Type MapType(string? typeString)
    {
        if (string.IsNullOrEmpty(typeString))
            return typeof(TextElement);

        // Web 短格式: template.element.text
        if (typeString.StartsWith("template.element."))
        {
            var shortName = typeString["template.element.".Length..];
            if (WebShortTypeMap.TryGetValue(shortName, out var type))
                return type;
        }

        // WPF 完整格式: Xinglin.Core.Elements.TextElement, Xinglin.Core
        var parts = typeString.Split(',')[0].Trim();
        var simpleName = parts.Split('.').Last();
        
        return simpleName switch
        {
            "TextElement" or "LabelElement" or "LabelInputBoxElement" => typeof(TextElement),
            "LineElement" => typeof(LineElement),
            "TableElement" => typeof(TableElement),
            "NumberElement" => typeof(NumberElement),
            "DateElement" => typeof(DateElement),
            "DropdownElement" => typeof(DropdownElement),
            "ImageElement" => typeof(ImageElement),
            "RectangleElement" or "EllipseElement" => typeof(ShapeElement),
            "BarcodeElement" => typeof(BarcodeElement),
            "QrCodeElement" => typeof(QrCodeElement),
            "SignatureElement" => typeof(SignatureElement),
            "CheckboxElement" => typeof(CheckboxElement),
            "RadioElement" => typeof(RadioElement),
            "AutoNumberElement" => typeof(PageNumberElement),
            "DividerElement" => typeof(DividerElement),
            "IconElement" => typeof(IconElement),
            "HyperlinkElement" => typeof(HyperlinkElement),
            "ChartElement" => typeof(ChartElement),
            "ContainerElement" => typeof(ContainerElement),
            "RepeatElement" => typeof(RepeatElement),
            "HeaderElement" => typeof(HeaderElement),
            "FooterElement" => typeof(FooterElement),
            "WatermarkElement" => typeof(WatermarkElement),
            _ => typeof(TextElement)
        };
    }

    private static ElementGroup ClassifyElement(ExternalElementBase element)
    {
        // Context 前缀
        if (element.DataPath?.StartsWith("Context.", StringComparison.OrdinalIgnoreCase) == true)
            return ElementGroup.Context;
        
        if (!element.IsDataBound)
        {
            if (element is TextElement textEl && !string.IsNullOrEmpty(textEl.Text))
                return ElementGroup.Fixed;
            if (element is LineElement)
                return ElementGroup.Fixed;
            return ElementGroup.Fixed;
        }

        if (element is TableElement or RepeatElement or ChartElement)
            return ElementGroup.DataAdapter;

        return ElementGroup.Editable;
    }
}
```

### 阶段三：辅助类和注册表（Day 6）

#### Step 3.1：创建元素注册表

**新建文件**：`Contracts/Registry/ElementGroupRegistry.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;
using Xinglin.ReportEditor.Contracts.Models.Elements;

namespace Xinglin.ReportEditor.Contracts.Registry;

public static class ElementGroupRegistry
{
    private static readonly Dictionary<ElementAdaptationGroup, HashSet<Type>> _groupElements = new();
    
    static ElementGroupRegistry()
    {
        RegisterGroup(ElementAdaptationGroup.Basic, 
            typeof(LineElement), typeof(DividerElement), typeof(ShapeElement));
        
        RegisterGroup(ElementAdaptationGroup.Form,
            typeof(TextElement), typeof(NumberElement), typeof(DateElement),
            typeof(DropdownElement), typeof(CheckboxElement), typeof(RadioElement));
        
        RegisterGroup(ElementAdaptationGroup.Data,
            typeof(TableElement), typeof(RepeatElement), typeof(ChartElement));
        
        RegisterGroup(ElementAdaptationGroup.Advanced,
            typeof(ImageElement), typeof(BarcodeElement), typeof(QrCodeElement),
            typeof(SignatureElement), typeof(HyperlinkElement), typeof(IconElement),
            typeof(ContainerElement), typeof(HeaderElement), typeof(FooterElement),
            typeof(PageNumberElement), typeof(WatermarkElement));
    }

    private static void RegisterGroup(ElementAdaptationGroup group, params Type[] types)
    {
        _groupElements[group] = new HashSet<Type>(types);
    }

    public static ElementAdaptationGroup GetGroup(Type elementType)
    {
        foreach (var kvp in _groupElements)
        {
            if (kvp.Value.Contains(elementType))
                return kvp.Key;
        }
        return ElementAdaptationGroup.Basic;
    }

    public static IEnumerable<Type> GetElementsInGroup(ElementAdaptationGroup group)
    {
        return _groupElements.GetValueOrDefault(group, new HashSet<Type>());
    }
}
```

#### Step 3.2：创建 Fallback 适配机制

**新建文件**：`Contracts/Models/Adapters/FallbackAdapter.cs`

```csharp
using Xinglin.ReportEditor.Contracts.Enums;
using Xinglin.ReportEditor.Contracts.Models.Adapters;

namespace Xinglin.ReportEditor.Contracts.Models.Adapters;

public class FallbackAdapter : IDataAdapter
{
    public string AdapterId => "fallback";
    public string AdapterName => "自动适配";
    public AdapterType Type => AdapterType.Context;
    
    public IReadOnlyList<string> TargetDataPaths => Array.Empty<string>();

    public Task<AdapterResult> ReadDataAsync()
    {
        return Task.FromResult(new AdapterResult { Success = true });
    }

    public Task<AdapterResult> ReadBatchDataAsync()
    {
        return Task.FromResult(new AdapterResult { Success = true });
    }

    public Task<ValidationResult> ValidateConfigAsync()
    {
        return Task.FromResult(ValidationResult.Success);
    }
}
```

### 阶段四：ReportDataMaker 重构（Day 7-9）

#### Step 4.1：更新项目引用

修改 `ReportDataMaker.csproj`：

```xml
<ItemGroup>
    <ProjectReference Include="..\..\Contracts\Xinglin.ReportEditor.Contracts.csproj" />
</ItemGroup>
```

#### Step 4.2：删除 Legacy 模型文件

删除以下文件（已迁移到 Contracts）：
- `Models/ElementBase.cs`
- `Models/ExternalElementBase.cs`
- `Models/ExternalLineElement.cs`
- `Models/ExternalTextElement.cs`
- `Models/ExternalTableElement.cs`
- `Models/ExternalNumberElement.cs`
- `Models/ExternalDateElement.cs`
- `Models/ExternalDropdownElement.cs`
- `Models/ExternalTemplateModels.cs`
- `Models/IDataAdapter.cs`
- `Models/DbAdapterConfig.cs`

#### Step 4.3：更新服务引用

修改所有引用旧模型的文件，更新命名空间：

```csharp
// 旧
using ReportDataMaker.Models;

// 新
using Xinglin.ReportEditor.Contracts.Models.Elements;
using Xinglin.ReportEditor.Contracts.Models.Template;
using Xinglin.ReportEditor.Contracts.Models.Adapters;
using Xinglin.ReportEditor.Contracts.Enums;
```

#### Step 4.4：更新元素转换器引用

修改 `Services/ExternalElementConverter.cs`：

```csharp
// 删除本地转换器逻辑
// 改为直接使用 Contracts 中的 ElementJsonConverter

var settings = new JsonSerializerSettings
{
    Converters = new JsonConverter[] 
    { 
        new ElementJsonConverter() 
    }
};
```

### 阶段五：Editor.Core 对齐（Day 10）

#### Step 5.1：检查 SharedInterfaces 对齐

检查 `Editor/Core/SharedInterfaces/` 中的接口是否需要更新以对齐新 Contracts 模型。

#### Step 5.2：验证依赖关系

确保 Editor.Server → Editor.Core → Contracts 的依赖链正确。

### 阶段六：测试与验证（Day 11-12）

#### Step 6.1：编译验证

```bash
cd ReportPlatform
dotnet build
```

#### Step 6.2：模板加载测试

验证以下模板能正确加载：
- 检验报告单.json
- 影像报告单.json
- 处方单.json
- 门诊病历.json

#### Step 6.3：双格式兼容测试

- [ ] WPF 完整格式模板加载正常
- [ ] Web 短格式模板加载正常
- [ ] 混合格式模板加载正常

#### Step 6.4：元素分组测试

验证 23 种元素的 Group 分类正确。

---

## 4. 文件变更清单

### 新建文件（Contracts 项目）

```
Contracts/
├── Contracts.csproj
├── Enums/
│   ├── ElementGroup.cs
│   ├── ElementAdaptationGroup.cs
│   └── AdapterType.cs
├── Models/
│   ├── Elements/
│   │   ├── ElementBase.cs
│   │   ├── ExternalElementBase.cs
│   │   ├── LineElement.cs
│   │   ├── TextElement.cs
│   │   ├── ImageElement.cs
│   │   ├── TableElement.cs
│   │   ├── NumberElement.cs
│   │   ├── DateElement.cs
│   │   ├── DropdownElement.cs
│   │   ├── CheckboxElement.cs
│   │   ├── RadioElement.cs
│   │   ├── BarcodeElement.cs
│   │   ├── QrCodeElement.cs
│   │   ├── SignatureElement.cs
│   │   ├── ShapeElement.cs
│   │   ├── ContainerElement.cs
│   │   ├── RepeatElement.cs
│   │   ├── HeaderElement.cs
│   │   ├── FooterElement.cs
│   │   ├── PageNumberElement.cs
│   │   ├── WatermarkElement.cs
│   │   ├── IconElement.cs
│   │   ├── HyperlinkElement.cs
│   │   ├── DividerElement.cs
│   │   └── ChartElement.cs
│   ├── Template/
│   │   ├── TemplateDefinition.cs
│   │   ├── PageSettings.cs
│   │   └── DataBindingDefinition.cs
│   └── Adapters/
│       ├── IDataAdapter.cs
│       ├── AdapterResult.cs
│       ├── AdapterConfigBase.cs
│       ├── FieldSchema.cs
│       └── FallbackAdapter.cs
├── Converters/
│   └── ElementJsonConverter.cs
└── Registry/
    └── ElementGroupRegistry.cs
```

### 删除文件（ReportDataMaker 项目）

```
ReportDataMaker/Models/
├── ElementBase.cs
├── ExternalElementBase.cs
├── ExternalLineElement.cs
├── ExternalTextElement.cs
├── ExternalTableElement.cs
├── ExternalNumberElement.cs
├── ExternalDateElement.cs
├── ExternalDropdownElement.cs
├── ExternalTemplateModels.cs
├── IDataAdapter.cs
└── DbAdapterConfig.cs
```

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `ReportDataMaker/ReportDataMaker.csproj` | 添加 Contracts 项目引用 |
| `ReportDataMaker/Services/*.cs` | 更新命名空间引用 |
| `ReportDataMaker/ViewModels/*.cs` | 更新命名空间引用 |
| `Contracts/Contracts.csproj` | 配置 .NET 8, 启用可空引用 |

---

## 5. 验收标准

### 5.1 编译通过 ⚠️ 待验证

- [ ] `dotnet build` 无错误
- [ ] 无警告（Warning）

### 5.2 模板加载正常 ⚠️ 待验证

- [ ] 7 个内置模板全部加载成功
- [ ] 元素数量和类型与定义一致
- [ ] 元素分组正确

### 5.3 双格式兼容 ⚠️ 待验证

- [ ] WPF 完整格式模板正常加载
- [ ] Web 短格式模板正常加载
- [ ] 混合格式模板正常加载

### 5.4 依赖关系正确 ⚠️ 待验证

- [ ] ReportDataMaker 仅依赖 Contracts
- [ ] 无循环依赖
- [ ] Editor.Core 依赖 Contracts

### 5.5 代码质量

- [x] 所有新建类添加 XML 文档注释
- [x] 枚举值添加描述
- [x] 关键方法添加说明

---

## 6. TODO 与优化项

> 记录实施过程中的优化点和待改进事项

### 6.1 唯一 ID 长度优化

- **问题**：Guid.NewGuid() 生成的 ID 长度约 36 字符（含连字符），增加 JSON 文件大小
- **建议方案**：
  1. 使用 `Guid.NewGuid().ToString("N")` - 32 字符（无连字符）
  2. 使用时间戳 + 随机数组合 - 更短但需确保唯一性
  3. 使用 NanoID 库 - 更短的 URL-safe 唯一 ID
- **状态**：✅ 已实现 - 当前使用 `Guid.NewGuid().ToString("N")`

### 6.2 性能优化

- [ ] 考虑为 ElementGroupRegistry 添加静态构造函数缓存
- [ ] 考虑为 WebShortTypeMap 添加线程安全版本

### 6.3 未来优化

- [ ] 支持元素 ID 的 Base64 编码格式（更短）
- [ ] 添加 ID 生成器接口，允许自定义 ID 策略

---

## 7. 风险与对策

### 5.1 编译通过

- [ ] `dotnet build` 无错误
- [ ] 无警告（Warning）

### 5.2 模板加载正常

- [ ] 7 个内置模板全部加载成功
- [ ] 元素数量和类型与定义一致
- [ ] 元素分组正确

### 5.3 双格式兼容

- [ ] WPF 完整格式模板正常加载
- [ ] Web 短格式模板正常加载
- [ ] 混合格式模板正常加载

### 5.4 依赖关系正确

- [ ] ReportDataMaker 仅依赖 Contracts
- [ ] 无循环依赖
- [ ] Editor.Core 依赖 Contracts

### 5.5 代码质量

- [ ] 所有新建类添加 XML 文档注释
- [ ] 枚举值添加描述
- [ ] 关键方法添加说明

---

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 命名空间变更导致大量引用需要更新 | 工作量大 | 分阶段执行，每阶段验证 |
| 双格式转换器边界情况未覆盖 | 部分模板可能加载失败 | 增加测试用例覆盖 |
| 遗留代码清理不完整 | 代码冗余，维护困难 | 最后进行全局搜索确认 |

---

## 8. 后续版本依赖

v0.3.0 是基础版本，后续版本依赖关系：

```
v0.3.0 (架构统一)
    │
    ├── v0.4.0 (WPF MVVM 重构) - 依赖 Contracts 模型
    │
    ├── v0.5.0 (Excel 适配器) - 依赖 IDataAdapter 接口
    │
    └── v0.6.0 (数据库适配器) - 依赖 AdapterType 枚举
```

---

*本文档为 v0.3.0 详细设计和开发计划*
