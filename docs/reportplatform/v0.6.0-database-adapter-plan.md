# v0.6.0 — 数据库适配器 (Database Adapter)

> 版本：v0.6.0
> 日期：2026-05-08
> 状态：开发中
> 前置版本：v0.5.0 (Excel 适配器)

---

## 1. 目标

将现有 `DatabaseAdapter` 重构为 **Base + Provider** 分层架构，实现：

- 可插拔的数据库 Provider（SqlServer / MySql / Sqlite / PostgreSql）
- 可视化查询构建 + 原生 SQL 双模式
- 参数化查询与参数来源定义
- 数据库列 → 模板 DataPath 字段映射
- 连接测试、元数据浏览（表/列/外键）
- WPF Tab 页签配置 UI

---

## 2. 架构设计

### 2.1 分层结构

```
┌─────────────────────────────────────────────────┐
│           DatabaseAdapterBase                    │
│  - 查询执行引擎                                  │
│  - 字段映射管理                                  │
│  - 参数化查询                                    │
│  - 结果转换 → AdapterResult                      │
├─────────────────────────────────────────────────┤
│         IDatabaseProvider                        │
├──────────┬──────────┬──────────┬────────────────┤
│SqlServer │  MySql   │  Sqlite  │  PostgreSql    │
│Provider  │ Provider │ Provider │  Provider      │
└──────────┴──────────┴──────────┴────────────────┘
```

### 2.2 与现有代码的关系

| 现有实现 | 重构方向 |
|---------|---------|
| `DatabaseAdapter` (DbProviderFactory) | 拆分为 `DatabaseAdapterBase` + `IDatabaseProvider` 实现 |
| `DbAdapterConfig` (简单字符串) | 演化为 `DatabaseAdapterConfig`（枚举化 Provider + QueryConfig） |
| `DataAdapterService` (注册表) | 保留，后续统一为 `AdapterRegistry` |
| `DbMappingEntry` | 演化为 `DbFieldMapping`（增加 Transform） |

---

## 3. 文件规划

### 3.1 Services/DatabaseAdapter/ 目录

| 文件 | 职责 |
|------|------|
| `IDatabaseProvider.cs` | Provider 接口定义 |
| `TableInfo.cs` | 元数据模型（TableInfo, ColumnInfo, ForeignKeyInfo） |
| `SqlServerProvider.cs` | SQL Server Provider |
| `MySqlProvider.cs` | MySQL Provider |
| `SqliteProvider.cs` | SQLite Provider |
| `PostgreSqlProvider.cs` | PostgreSQL Provider |
| `DatabaseProviderRegistry.cs` | Provider 注册表 |
| `DatabaseAdapterConfig.cs` | 数据库适配器配置模型 |
| `DatabaseAdapterBase.cs` | 适配器基类（查询执行 + 映射） |
| `DatabaseAdapterFactory.cs` | 工厂（整合 Provider + AdapterBase） |
| `SqlBuilder.cs` | SQL 构建辅助（可视化模式 → SQL） |

### 3.2 ViewModels/Tabs/ 目录

| 文件 | 职责 |
|------|------|
| `DatabaseAdapterTabViewModel.cs` | 数据库适配器 Tab ViewModel |

### 3.3 Views/Tabs/ 目录

| 文件 | 职责 |
|------|------|
| `DatabaseAdapterTab.xaml` | 数据库适配器 Tab UI |
| `DatabaseAdapterTab.xaml.cs` | 代码隐藏 |

---

## 4. 核心模型设计

### 4.1 IDatabaseProvider

```csharp
public interface IDatabaseProvider
{
    DatabaseProvider ProviderType { get; }
    string DisplayName { get; }

    Task<bool> TestConnectionAsync(string connectionString);
    DbConnection CreateConnection(string connectionString);
    Task<List<TableInfo>> GetTablesAsync(string connectionString);
    Task<List<ColumnInfo>> GetColumnsAsync(string connectionString, string tableName);
    Task<List<ForeignKeyInfo>> GetForeignKeysAsync(string connectionString, string tableName);
    string BuildPagedQuery(string baseSql, int offset, int limit);
    string QuoteIdentifier(string identifier);
    string GetParameterPrefix();
}
```

### 4.2 元数据模型

```csharp
public class TableInfo
{
    public string Schema { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
    public string Type { get; set; } = string.Empty;  // "TABLE", "VIEW"
}

public class ColumnInfo
{
    public string Name { get; set; } = string.Empty;
    public string DataType { get; set; } = string.Empty;
    public bool IsNullable { get; set; }
    public bool IsPrimaryKey { get; set; }
    public int? MaxLength { get; set; }
}

public class ForeignKeyInfo
{
    public string ColumnName { get; set; } = string.Empty;
    public string ReferencedTable { get; set; } = string.Empty;
    public string ReferencedColumn { get; set; } = string.Empty;
}
```

### 4.3 DatabaseAdapterConfig

```csharp
public class DatabaseAdapterConfig : AdapterConfigBase
{
    public DatabaseProvider Provider { get; set; }
    public string ConnectionString { get; set; } = string.Empty;
    public QueryConfig Query { get; set; } = new();
    public List<DbFieldMapping> DbFieldMappings { get; set; } = new();
    public List<QueryParameter> Parameters { get; set; } = new();
    public List<JoinDefinition> Joins { get; set; } = new();
}

public class QueryConfig
{
    public QueryMode Mode { get; set; } = QueryMode.RawSql;
    public string RawSql { get; set; } = string.Empty;
    public string PrimaryTable { get; set; } = string.Empty;
    public List<string> SelectedColumns { get; set; } = new();
    public string WhereClause { get; set; } = string.Empty;
    public string OrderBy { get; set; } = string.Empty;
}

public enum QueryMode { RawSql, VisualBuilder }

public class DbFieldMapping
{
    public string ColumnName { get; set; } = string.Empty;
    public string TargetDataPath { get; set; } = string.Empty;
    public string Transform { get; set; } = string.Empty;
}

public class QueryParameter
{
    public string Name { get; set; } = string.Empty;
    public string Label { get; set; } = string.Empty;
    public FieldDataType DataType { get; set; } = FieldDataType.Text;
    public string DefaultValue { get; set; } = string.Empty;
    public ParameterSource Source { get; set; } = ParameterSource.Manual;
}

public enum ParameterSource { Manual, FromTemplate, FromContext }

public class JoinDefinition
{
    public string LeftTable { get; set; } = string.Empty;
    public string LeftColumn { get; set; } = string.Empty;
    public string RightTable { get; set; } = string.Empty;
    public string RightColumn { get; set; } = string.Empty;
    public JoinType Type { get; set; } = JoinType.Inner;
}

public enum JoinType { Inner, Left, Right }
```

### 4.4 DatabaseAdapterBase

```csharp
public class DatabaseAdapterBase
{
    private readonly IDatabaseProvider _provider;
    private readonly DatabaseAdapterConfig _config;

    public async Task<AdapterResult> ReadDataAsync() { ... }
    public async Task<AdapterResult> ReadBatchDataAsync() { ... }
    public Task<ValidationResult> ValidateConfigAsync() { ... }

    private string BuildSql() { ... }
    private void ApplyParameters(DbCommand cmd) { ... }
    private AdapterResult MapResult(DbDataReader reader) { ... }
}
```

### 4.5 SqlBuilder

```csharp
public class SqlBuilder
{
    public string Build(QueryConfig query, List<JoinDefinition> joins, IDatabaseProvider provider) { ... }
}
```

---

## 5. Provider 实现要点

### 5.1 SqlServerProvider

- 连接：`Microsoft.Data.SqlClient.SqlConnection`
- 元数据：`INFORMATION_SCHEMA.TABLES` / `COLUMNS` / `KEY_COLUMN_USAGE`
- 标识符引用：`[name]`
- 参数前缀：`@`
- 分页：`OFFSET ... ROWS FETCH NEXT ... ROWS ONLY`

### 5.2 MySqlProvider

- 连接：`MySqlConnector.MySqlConnection`
- 元数据：`SHOW TABLES` / `SHOW COLUMNS FROM` / `INFORMATION_SCHEMA`
- 标识符引用：`` `name` ``
- 参数前缀：`@`
- 分页：`LIMIT ... OFFSET ...`

### 5.3 SqliteProvider

- 连接：`Microsoft.Data.Sqlite.SqliteConnection`
- 元数据：`sqlite_master` / `PRAGMA table_info` / `PRAGMA foreign_key_list`
- 标识符引用：`"name"`
- 参数前缀：`@`
- 分页：`LIMIT ... OFFSET ...`

### 5.4 PostgreSqlProvider

- 连接：`Npgsql.NpgsqlConnection`
- 元数据：`information_schema.tables` / `columns` / `table_constraints`
- 标识符引用：`"name"`
- 参数前缀：`:`
- 分页：`LIMIT ... OFFSET ...`

---

## 6. ViewModel 设计

### 6.1 DatabaseAdapterTabViewModel

```
属性:
- SelectedProvider: DatabaseProvider
- ConnectionString: string
- IsConnected: bool
- Tables: ObservableCollection<TableInfo>
- SelectedTable: TableInfo?
- Columns: ObservableCollection<ColumnInfo>
- QueryMode: QueryMode
- RawSql: string
- PrimaryTable: string
- WhereClause: string
- OrderBy: string
- Joins: ObservableCollection<JoinDefinition>
- Parameters: ObservableCollection<QueryParameter>
- DbFieldMappings: ObservableCollection<DbFieldMapping>
- TemplateFields: List<FlatField>  (来自 TemplateFlattenService)
- PreviewData: DataTable
- StatusText: string

命令:
- TestConnectionCommand: AsyncRelayCommand
- LoadTablesCommand: AsyncRelayCommand
- LoadColumnsCommand: AsyncRelayCommand
- ExecutePreviewCommand: AsyncRelayCommand
- ExecuteQueryCommand: AsyncRelayCommand
- AddJoinCommand: RelayCommand
- RemoveJoinCommand: RelayCommand
- AddParameterCommand: RelayCommand
- RemoveParameterCommand: RelayCommand
- AutoMatchCommand: RelayCommand  (自动匹配列名 → DataPath)
- SaveConfigCommand: RelayCommand
```

---

## 7. UI 设计

```
┌─────────────────────────────────────────────────────────────┐
│ 数据库适配器                                    [TabItem]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─ 连接配置 ──────────────────────────────────────────────┐ │
│ │ 数据库类型: [SQL Server ▼]                               │ │
│ │ 连接字符串: [________________________________]           │ │
│ │ [测试连接]  状态: ● 已连接 / ○ 未连接                    │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─ 查询构建 ──────────────────────────────────────────────┐ │
│ │ 模式: ○ 可视化构建  ● SQL 语句                           │ │
│ │                                                         │ │
│ │ [SQL 模式]                                              │ │
│ │ ┌─────────────────────────────────────────────────────┐ │ │
│ │ │ SELECT p.Name, p.Age, r.Result                      │ │ │
│ │ │ FROM Patients p                                     │ │ │
│ │ │ WHERE p.Id = @patientId                             │ │ │
│ │ └─────────────────────────────────────────────────────┘ │ │
│ │ [执行预览 (前10行)]                                      │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─ 参数定义 ──────────────────────────────────────────────┐ │
│ │ @patientId  类型:[String ▼]  来源:[手动输入 ▼]  默认:[] │ │
│ │ [+ 添加参数]                                            │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─ 字段映射 ──────────────────────────────────────────────┐ │
│ │ ┌────────────┬──────────────┬────────────┐              │ │
│ │ │ 查询结果列  │ 模板字段      │ 转换       │              │ │
│ │ ├────────────┼──────────────┼────────────┤              │ │
│ │ │ Name       │ Patient.Name ▼│            │              │ │
│ │ │ Age        │ Patient.Age  ▼│            │              │ │
│ │ └────────────┴──────────────┴────────────┘              │ │
│ │ [自动匹配]                                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [保存配置]  [执行查询并填充]                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 实施阶段

### Phase 1: Provider 层 (2天)
- [ ] IDatabaseProvider 接口 + 元数据模型
- [ ] SqlServerProvider
- [ ] MySqlProvider
- [ ] SqliteProvider
- [ ] PostgreSqlProvider
- [ ] DatabaseProviderRegistry

### Phase 2: 适配器核心 (2天)
- [ ] DatabaseAdapterConfig (QueryConfig, DbFieldMapping, QueryParameter, JoinDefinition)
- [ ] SqlBuilder (可视化模式 → SQL)
- [ ] DatabaseAdapterBase (查询执行 + 映射 + 验证)

### Phase 3: 工厂与集成 (1天)
- [ ] DatabaseAdapterFactory
- [ ] MainViewModel 集成（添加数据库适配器 Tab）
- [ ] App.xaml.cs DI 注册

### Phase 4: ViewModel (2天)
- [ ] DatabaseAdapterTabViewModel（连接/查询/参数/映射/预览）

### Phase 5: View (2天)
- [ ] DatabaseAdapterTab.xaml + .cs

### Phase 6: 验证与文档 (1天)
- [ ] 编译验证 (⚠️ 待 .NET SDK 可用)
- [ ] 更新文档

---

## 9. 依赖

| 包 | 用途 | 已有 |
|----|------|------|
| Microsoft.Data.SqlClient | SQL Server | ✅ |
| MySqlConnector | MySQL | ✅ |
| Microsoft.Data.Sqlite | SQLite | ✅ |
| Npgsql | PostgreSQL | ❌ 需新增 |

---

## 10. TODO

- [ ] 短唯一ID：考虑使用更短的 ID 方案替代 Guid（参见 v0.3.0 TODO）
- [ ] 连接字符串加密：敏感信息不应明文存储
- [ ] 可视化查询构建器：Phase 1 仅支持 SQL 模式，可视化模式后续迭代
- [ ] 连接池管理：多适配器共享连接
