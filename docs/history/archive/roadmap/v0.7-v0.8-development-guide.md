# v0.7.0 报告输出增强 & v0.8.0 上下文适配器 — 开发文档

> 版本：v1.0 | 编写日期：2026-05-08
> 前置版本：v0.6.0（数据库适配器已完成）
> 说明：API 适配器暂不纳入当前迭代

---

## 1. 版本概览

| 版本 | 主题 | 核心交付 |
|------|------|---------|
| v0.7.0 | 报告输出增强 | PDF 导出、批量生成、打印预览 |
| v0.8.0 | 上下文适配器 | 自动填充医院/医生/日期等上下文字段 |

执行顺序建议：v0.8.0 → v0.7.0（上下文填充是数据准备阶段，输出是最终环节）

---

## 2. 架构基础（现有可复用组件）

### 2.1 适配器执行链

```
Context 适配器(v0.8.0) ──▶ DataAdapter 适配器 ──▶ Editable 手动录入 ──▶ 报告输出(v0.7.0)
     (自动填充)              (Excel/数据库)          (人工补充)            (PDF/打印)
```

### 2.2 关键现有接口

| 接口/类 | 路径 | 用途 |
|---------|------|------|
| `IDataAdapter` | `Contracts/Models/Adapters/IDataAdapter.cs` | 适配器契约 |
| `AdapterConfigBase` | `Contracts/Models/Adapters/AdapterConfigBase.cs` | 配置基类 |
| `AdapterResult` | `Contracts/Models/Adapters/AdapterResult.cs` | 执行结果 |
| `AdapterType.Context` | `Contracts/Enums/AdapterType.cs` | 已定义枚举值 |
| `ElementGroup.Context` | `Contracts/Enums/ElementGroup.cs` | 已定义分组 |
| `ITemplatePreviewService` | `Services/ITemplatePreviewService.cs` | 预览服务接口 |
| `ExternalTemplateDefinition` | `Models/ExternalTemplateModels.cs` | 模板定义 |
| `IPdfSharpTemplateRenderer` | `Editor/Core/SharedInterfaces/` | PDF 渲染接口（Stub） |

### 2.3 元素分组规则（ClassifyElement）

```
DataPath 以 "Context." 开头 → ElementGroup.Context（v0.8.0 目标）
有 AdapterId → ElementGroup.DataAdapter
Table/Repeat/Chart → ElementGroup.DataAdapter
其余有 DataPath → ElementGroup.Editable
```

---

## 3. v0.7.0 — 报告输出增强

### 3.1 目标

将填充完成的模板数据渲染为 PDF 文件，支持批量生成和打印预览。

### 3.2 技术选型

| 方案 | 优势 | 劣势 | 推荐 |
|------|------|------|------|
| QuestPDF | 流式 API、现代 C#、MIT 协议 | 社区版功能足够 | **推荐** |
| PdfSharp | 老牌稳定 | API 较底层，布局需手写 | 备选 |
| iText7 | 功能最全 | AGPL 协议，商用需付费 | 不推荐 |

### 3.3 新增文件清单

```
Generators/ReportDataMaker/
├── Services/PdfExport/
│   ├── PdfExportService.cs            # PDF 渲染核心
│   ├── PdfElementRenderer.cs          # 元素 → PDF 组件映射
│   ├── PdfPageLayoutEngine.cs         # 页面布局引擎
│   ├── BatchExportService.cs          # 批量生成服务
│   └── ExportHistoryStore.cs          # 导出历史记录
├── ViewModels/Tabs/
│   └── ExportTabViewModel.cs          # 导出 Tab ViewModel
└── Views/Tabs/
    ├── ExportTab.xaml                  # 导出 Tab UI
    └── ExportTab.xaml.cs
```

### 3.4 PdfExportService 设计

```csharp
public interface IPdfExportService
{
    byte[] RenderToPdf(ExternalTemplateDefinition template, Dictionary<string, object> data);
    Task<List<string>> BatchExportAsync(
        ExternalTemplateDefinition template,
        List<Dictionary<string, object>> batchData,
        string outputDirectory,
        string fileNamePattern,
        IProgress<int>? progress = null);
}
```

### 3.5 PdfElementRenderer（元素渲染映射）

每种元素类型对应一个 PDF 渲染策略：

| 元素类型 | PDF 渲染方式 |
|---------|-------------|
| TextElement | 文本块（字体/大小/颜色/对齐） |
| NumberElement | 格式化数字文本 |
| DateElement | 格式化日期文本 |
| ImageElement | 嵌入图片 |
| TableElement | PDF 表格（行列/边框/合并） |
| BarcodeElement | ZXing 生成条码图片嵌入 |
| QrCodeElement | ZXing 生成二维码图片嵌入 |
| LineElement | 绘制线条 |
| ShapeElement | 绘制矩形/椭圆 |
| DividerElement | 水平分隔线 |
| CheckboxElement | ☑/☐ 符号 |
| RadioElement | ◉/○ 符号 |
| SignatureElement | 签名图片嵌入 |
| HeaderElement | 页眉区域 |
| FooterElement | 页脚区域 |
| PageNumberElement | 页码文本 |
| WatermarkElement | 半透明水印层 |

### 3.6 BatchExportService

```csharp
public class BatchExportService
{
    // 批量生成流水线：
    // 1. 接收 BatchData（来自 Excel/Database 适配器）
    // 2. 逐行填充模板
    // 3. 渲染 PDF
    // 4. 按命名规则保存文件
    // 5. 报告进度

    public async Task<BatchExportResult> ExportAsync(
        ExternalTemplateDefinition template,
        List<Dictionary<string, object>> batchData,
        BatchExportOptions options)
    {
        // options: OutputDir, FileNamePattern("{index}_{PatientName}"), Parallelism
    }
}
```

### 3.7 ExportTabViewModel UI 区域

| 区域 | 功能 |
|------|------|
| 单份导出 | 预览当前数据 → 导出 PDF → 选择保存路径 |
| 批量导出 | 选择数据源（Excel/数据库结果）→ 命名规则 → 输出目录 → 开始 |
| 进度 | 进度条 + 当前/总数 + 预计剩余时间 |
| 打印 | 打印预览 → 选择打印机 → 打印（使用 WPF PrintDialog） |
| 历史 | 最近导出记录列表（时间/文件名/路径） |

### 3.8 打印预览

利用 WPF 内置 `PrintDialog` + `DocumentPaginator`：

```csharp
public class ReportDocumentPaginator : DocumentPaginator
{
    // 将 PDF 页面转为 WPF Visual 用于打印预览
    // 或直接使用 QuestPDF 的页面模型生成 FixedDocument
}
```

### 3.9 NuGet 依赖

```xml
<PackageReference Include="QuestPDF" Version="2025.1.0" />
```

### 3.10 DI 注册

```csharp
services.AddSingleton<IPdfExportService, PdfExportService>();
services.AddSingleton<BatchExportService>();
services.AddSingleton<ExportHistoryStore>();
```

---

## 4. v0.8.0 — 上下文适配器

### 4.1 目标

自动填充报告中 `DataPath` 以 `Context.` 开头的元素，数据来源为本地配置文件和系统环境。

### 4.2 Context 数据源分类

| 数据源 | DataPath 前缀 | 示例 |
|--------|--------------|------|
| 医院信息 | `Context.Hospital.*` | Context.Hospital.Name, Context.Hospital.Address |
| 科室信息 | `Context.Department.*` | Context.Department.Name, Context.Department.Code |
| 医生信息 | `Context.Doctor.*` | Context.Doctor.Name, Context.Doctor.Title |
| 日期时间 | `Context.DateTime.*` | Context.DateTime.Now, Context.DateTime.Date |
| 患者信息 | `Context.Patient.*` | Context.Patient.Name, Context.Patient.Id |
| 自定义 | `Context.Custom.*` | Context.Custom.ReportNo |

### 4.3 新增文件清单

```
Generators/ReportDataMaker/
├── Services/ContextAdapter/
│   ├── ContextAdapterConfig.cs        # 上下文配置
│   ├── ContextDataSource.cs           # 数据源定义
│   ├── ContextAdapterService.cs       # 核心填充逻辑
│   ├── ContextAdapterFactory.cs       # 工厂
│   └── ContextProfileStore.cs         # 配置文件持久化
├── ViewModels/Tabs/
│   └── ContextAdapterTabViewModel.cs  # Tab ViewModel
└── Views/Tabs/
    ├── ContextAdapterTab.xaml          # Tab UI
    └── ContextAdapterTab.xaml.cs
```

### 4.4 ContextAdapterConfig 设计

```csharp
public class ContextAdapterConfig : AdapterConfigBase
{
    public string ProfileName { get; set; } = "default";
    public Dictionary<string, string> StaticValues { get; set; } = new();
    public List<DynamicContextRule> DynamicRules { get; set; } = new();
}

public class DynamicContextRule
{
    public string DataPath { get; set; } = string.Empty;
    public ContextValueSource Source { get; set; }
    public string? Format { get; set; }          // 日期格式等
}

public enum ContextValueSource
{
    Static,          // 从 StaticValues 字典取值
    CurrentDate,     // DateTime.Now
    CurrentTime,     // DateTime.Now.TimeOfDay
    CurrentDateTime, // DateTime.Now 完整
    CurrentUser,     // Environment.UserName
    MachineName,     // Environment.MachineName
    Custom           // 自定义表达式
}
```

### 4.5 ContextAdapterService 核心逻辑

```csharp
public class ContextAdapterService
{
    public AdapterResult FillContext(
        ExternalTemplateDefinition template,
        ContextAdapterConfig config)
    {
        var data = new Dictionary<string, object>();

        foreach (var element in template.Elements)
        {
            if (element.Group != ElementGroup.Context) continue;
            if (string.IsNullOrEmpty(element.DataPath)) continue;

            // 1. 检查 StaticValues
            if (config.StaticValues.TryGetValue(element.DataPath, out var staticVal))
            {
                data[element.DataPath] = staticVal;
                continue;
            }

            // 2. 检查 DynamicRules
            var rule = config.DynamicRules.FirstOrDefault(r => r.DataPath == element.DataPath);
            if (rule != null)
            {
                data[element.DataPath] = ResolveDynamic(rule);
                continue;
            }

            // 3. 内置默认规则
            data[element.DataPath] = ResolveBuiltIn(element.DataPath);
        }

        return new AdapterResult { Success = true, Data = data };
    }

    private object ResolveBuiltIn(string dataPath) => dataPath switch
    {
        "Context.DateTime.Now" => DateTime.Now.ToString("yyyy-MM-dd HH:mm"),
        "Context.DateTime.Date" => DateTime.Now.ToString("yyyy-MM-dd"),
        "Context.DateTime.Time" => DateTime.Now.ToString("HH:mm:ss"),
        "Context.DateTime.Year" => DateTime.Now.Year.ToString(),
        _ => string.Empty
    };
}
```

### 4.6 ContextProfileStore（配置文件）

存储路径：`%AppData%/ReportDataMaker/context-profiles.json`

```json
{
  "default": {
    "ProfileName": "default",
    "StaticValues": {
      "Context.Hospital.Name": "杏林医院",
      "Context.Hospital.Address": "北京市朝阳区XX路100号",
      "Context.Department.Name": "检验科",
      "Context.Doctor.Name": "张医生",
      "Context.Doctor.Title": "主治医师"
    },
    "DynamicRules": [
      { "DataPath": "Context.DateTime.Now", "Source": "CurrentDateTime", "Format": "yyyy-MM-dd HH:mm" },
      { "DataPath": "Context.DateTime.Date", "Source": "CurrentDate", "Format": "yyyy-MM-dd" }
    ]
  }
}
```

### 4.7 ContextAdapterTabViewModel UI 区域

| 区域 | 功能 |
|------|------|
| 配置文件选择 | 下拉选择 Profile（default/自定义）+ 新建/删除 |
| 静态值编辑 | DataGrid：DataPath ↔ Value 键值对编辑 |
| 动态规则 | DataGrid：DataPath ↔ Source ↔ Format |
| 预览 | 显示当前配置填充后的所有 Context 字段值 |
| 自动检测 | 扫描模板中所有 Context.* 元素，列出未配置项 |

### 4.8 执行时机

Context 适配器在模板加载后**自动执行**（不需要用户手动触发）：

```csharp
// MainViewModel.OnTemplateLoaded()
var contextConfig = _configStore.LoadContextProfile(template.Name);
var contextResult = _contextFactory.Fill(template, contextConfig);
_dataBindingService.ApplyData(template, contextResult.Data);
```

### 4.9 DI 注册

```csharp
services.AddSingleton<ContextProfileStore>();
services.AddSingleton<ContextAdapterFactory>();
```

---

## 5. 集成点

### 5.1 MainViewModel 变更

```csharp
// 模板加载后的执行链：
1. LoadTemplate()
2. ContextAdapter.Fill()          ← v0.8.0 新增
3. 用户操作 DataAdapter（Excel/数据库）
4. 用户手动填写 Editable 字段
5. ExportService.RenderToPdf()    ← v0.7.0 新增
```

### 5.2 MainWindow 菜单扩展

```xml
<MenuItem Header="导出">
    <MenuItem Header="导出 PDF" Command="{Binding ExportPdfCommand}"/>
    <MenuItem Header="批量导出..." Command="{Binding BatchExportCommand}"/>
    <MenuItem Header="打印预览" Command="{Binding PrintPreviewCommand}"/>
</MenuItem>
<MenuItem Header="上下文">
    <MenuItem Header="编辑上下文配置" Command="{Binding EditContextCommand}"/>
    <MenuItem Header="切换配置文件" Command="{Binding SwitchContextProfileCommand}"/>
</MenuItem>
```

### 5.3 DataTemplate 注册

```xml
<!-- MainWindow.xaml Resources -->
<DataTemplate DataType="{x:Type vmtabs:ExportTabViewModel}">
    <tabs:ExportTab/>
</DataTemplate>
<DataTemplate DataType="{x:Type vmtabs:ContextAdapterTabViewModel}">
    <tabs:ContextAdapterTab/>
</DataTemplate>
```

---

## 6. 实施计划

### Phase 1: v0.8.0 上下文适配器（预计 1 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 1 | 创建 ContextAdapterConfig + ContextDataSource | 配置模型 |
| 2 | 实现 ContextAdapterService | 核心填充逻辑 |
| 3 | 实现 ContextProfileStore | 配置持久化 |
| 4 | 实现 ContextAdapterFactory | DI 工厂 |
| 5 | 创建 ContextAdapterTabViewModel | Tab 交互逻辑 |
| 6 | 创建 ContextAdapterTab.xaml | UI 界面 |
| 7 | 集成到 MainViewModel（模板加载后自动执行） | 执行链 |
| 8 | 验证：加载模板后 Context 字段自动填充 | 功能验证 |

### Phase 2: v0.7.0 报告输出增强（预计 2 周）

| 步骤 | 任务 | 产出 |
|------|------|------|
| 1 | 添加 QuestPDF NuGet 依赖 | 环境准备 |
| 2 | 实现 PdfElementRenderer（逐元素类型） | 渲染映射 |
| 3 | 实现 PdfPageLayoutEngine | 页面布局 |
| 4 | 实现 PdfExportService | 单份 PDF 导出 |
| 5 | 实现 BatchExportService | 批量生成 |
| 6 | 实现 ExportHistoryStore | 历史记录 |
| 7 | 创建 ExportTabViewModel | Tab 交互 |
| 8 | 创建 ExportTab.xaml | UI 界面 |
| 9 | 实现打印预览（WPF PrintDialog） | 打印功能 |
| 10 | 集成到 MainViewModel + 菜单 | 完整流程 |
| 11 | 验证：数据填充 → PDF 导出 → 打印 | 端到端验证 |

---

## 7. 验收标准

### v0.8.0

- [ ] 模板加载后，所有 `Context.*` 元素自动填充
- [ ] 支持多 Profile 切换（不同医院/科室配置）
- [ ] 日期时间字段实时更新
- [ ] 未配置的 Context 字段显示为空（不报错）
- [ ] 配置持久化到 `context-profiles.json`

### v0.7.0

- [ ] 单份 PDF 导出，布局与模板设计一致
- [ ] 支持所有 23 种元素类型的 PDF 渲染
- [ ] 批量导出：Excel/数据库数据 → 多份 PDF
- [ ] 批量导出显示进度条
- [ ] 打印预览正确显示
- [ ] 导出历史可查看

---

## 8. 风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| QuestPDF 社区版限制 | 可能有页数/功能限制 | 评估后如不满足可切换 PdfSharp |
| 复杂表格渲染 | 合并单元格、嵌套表格 | 先支持简单表格，复杂场景后续迭代 |
| 大批量导出内存 | 1000+ 份 PDF 可能 OOM | 流式生成，逐份写入磁盘 |
| Context 配置迁移 | 用户已有模板无 Context 前缀 | 提供迁移工具或兼容旧 DataPath |

---

*本文档为 v0.7.0 和 v0.8.0 的开发指引，实施时可根据实际情况调整细节。*
